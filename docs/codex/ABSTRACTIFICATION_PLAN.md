# Abstractification Plan

Move generic graph semantics into a core runtime. Move platform observation into adapters. Move lifecycle and safety decisions into policies. Move configuration into HGRL manifests. Move rendering into views. Keep the original engine intact while the experimental runtime proves the model in parallel.
