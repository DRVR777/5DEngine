# game.html Functionality Migration Backlog

Generated: 2026-05-24T03:55:03.031Z

This is the source-of-truth backlog for moving behavior out of `game.html`.
It is generated from live repo state, not hand-maintained impressions.

Total game.html mount* subsystems: 165
Coverage counted by current audit semantics: 24/165

Important: `data/legacy/*.json` only counts bridge-hosted specs. It is not the full backlog.

## Tally

- DONE: 10
- FACET_OR_KIND: 58
- HOSTED_BIND_ONLY: 13
- HOSTED_SEMANTIC_PROVEN: 1
- UNHOSTED: 83

## Full Mount Backlog

| Mount | Status | Native target | Current bridge file | Required next action | Functionality summary |
|---|---|---|---|---|---|
| `mountAmmoPickupTick` | FACET_OR_KIND | `ammo-pickup` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. | const _vehicleRenderTick = mountVehicleRenderTick(); const _heroRegenTick = mountHeroRegenTick({ get: { heroHp: () => heroHp, maxHp: () => HERO_MAX_HP, perkMaxHpBonus: () => _perkMaxHpBonus, lastDamageT: () => heroLastDamageT, regenDelay: () => HERO_REGEN_DELAY, regenRate: () => HERO_REGEN_RATE, perkRegenBonus: () => _perkRegenBonus }, set: { heroHp: v => {  |
| `mountAmmoReloadTick` | UNHOSTED | `ammo-reload` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | const _screenMeshTick = mountScreenMeshTick({ actions: { getScreenMesh: () => window.ScreenMesh \|\| null, getBuildConsoleScreen: () => window._buildConsoleScreen \|\| null, getBuildConsoleMesh: () => window._buildConsoleMesh \|\| null, intersectMesh: (mesh) => { const _r = new THREE.Raycaster(); _r.setFromCamera({ x: 0, y: 0 }, camera); return _r.intersectObject( |
| `mountAppMultiplayerWiring` | UNHOSTED | `app-multiplayer` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. |  }, }); const _appMp = mountAppMultiplayerWiring({ THREE, scene, buildComputerApps, addDynamicIcons, mountComputerUI, createLanSession, mountDuelMode, mountMpBadge, eventBus: typeof EventBus !== "undefined" ? EventBus : null, state: { mpState: _mpState }, |
| `mountArmorShardTick` | FACET_OR_KIND | `armor-shard` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. | const _layerBldgName = id => ({ 2:"shop", 3:"tower", 4:"house", 5:"garage", 6:"diner", 7:"bank", 8:"park", 9:"studio" })[id] \|\| `L${id}`; const _layerTransTick = mountLayerTransitionTick({ get: { layerId: () => world.layerId }, actions: { boundaryAt: (u, v, blds) => world.boundaryAt(u, v, blds), bldgName: id => _layerBldgName(id), logTransition: (from, to) = |
| `mountArmorVestTick` | UNHOSTED | `armor-vest` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | const _speedOrbTick = mountSpeedOrbTick({ set: { speedBoostT: v => { _speedBoostT = v; } }, actions: { removeMesh: mesh => scene.remove(mesh), spawnParticles: (u, y, v, n, col, spd, size) => _spawnParticles(u, y, v, n, col, spd, size), playSfx: (str, vol) => playSfx(str, vol), showToast: (msg, type, dur) => showToast(msg, type, dur) } }); const _poisonPuddle |
| `mountAssetBootstrap` | UNHOSTED | `asset-bootstrap` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | const _origBuildToggle = () => {}; mountAssetBootstrap({ THREE, Loaders, scene, get: { gunMeshes: () => _gunMeshes, gunMount: () => _gunMount, carGroup: () => carGroup, carBody: () => carBody, pickupMeshes: () => (typeof pickupMeshes !== "undefined") ? pickupMeshes : null, pickups: () => pickups, |
| `mountBarrelSystem` | FACET_OR_KIND | `barrel` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. | const _tryDroneShoot = _shootSys.tryDroneShoot; const _getAimAngle = _shootSys.getAimAngle; const { makeBarrel: _makeBarrel, explodeBarrel: _explodeBarrel, barrels } = mountBarrelSystem({ THREE, scene, enemies, world, coinByType: _COIN_BY_TYPE, weaponDropMap: _WEAPON_DROP_MAP, get: { heroDead: () => _heroDead, heroHp: () => heroHp, dodgeT: () => _dodgeT, ene |
| `mountBossBarTick` | UNHOSTED | `boss-bar` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | const _combatHudTick = mountCombatHudTick(); const _clockHudTick = mountClockHudTick({ actions: { getDayNightHour: () => (typeof DayNight !== "undefined") ? DayNight.getHour() : null } }); const _vignetteTick = mountVignetteTick({ get: { vignetteAmt: () => _vignetteAmt }, set: { vignetteAmt: v => { _vignetteAmt = v; } } }); const _statBarsTick = mountStatBar |
| `mountBuilderUiRefresh` | UNHOSTED | `builder-ui-refresh` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. |  }); }, 250); const _refreshBuilderUI = mountBuilderUiRefresh({ get: { buildMode: () => buildMode, worldBuilder: () => worldBuilder }, actions: { renderSceneHierarchy: _renderSceneHierarchy, getScriptRunner: () => window.ScriptRunner \|\| null, }, }); let audioMixer = null; if (window.GTAAudio && window.WebAudioAdapter) { |
| `mountBulletEnemyHitFeedbackTick` | FACET_OR_KIND | `bullet-enemy-hit-feedback` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. | const _enemyFootstepTick = mountEnemyFootstepTick({ get: { enFsT: () => _enFsT }, set: { enFsT: v => { _enFsT = v; } }, actions: { getHeroPos: () => world.players.get("hero") \|\| null, getEnemyPos: id => world.players.get(id) \|\| null, playSfx: (str, vol) => playSfx(str, vol) } }); const _enemyBulletTick = mountEnemyBulletTick({ get: { heroPos: () => world.pla |
| `mountBulletEnemyKillTick` | FACET_OR_KIND | `bullet-enemy-kill` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. | const _grenadeWarnTick = mountGrenadeWarnTick({ actions: { getWarnEl: () => _dom.grenadeWarn \|\| null } }); const _enemyFootstepTick = mountEnemyFootstepTick({ get: { enFsT: () => _enFsT }, set: { enFsT: v => { _enFsT = v; } }, actions: { getHeroPos: () => world.players.get("hero") \|\| null, getEnemyPos: id => world.players.get(id) \|\| null, playSfx: (str, vol) |
| `mountBulletGeo` | FACET_OR_KIND | `bullet-geo` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. | let flashbangCount = 2; const smokeZones = []; let _mineCount = 2; const { bulletGeo, bulletMat } = mountBulletGeo({ THREE }); const damageFlashEl = document.getElementById("damageFlash"); let damageFlashUntil = 0; let _vignetteAmt = 0; // spring [0..1]: driven by hit impulses + low-HP pulse |
| `mountBulletPhysicsTick` | FACET_OR_KIND | `bullet-physics` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. | const _enemyBulletTick = mountEnemyBulletTick({ get: { heroPos: () => world.players.get("hero") \|\| null, heroHp: () => heroHp, heroArmor: () => heroArmor, dodgeT: () => _dodgeT, godMode: () => typeof Engine !== "undefined" && Engine.debug.godMode, armorAbsorb: () => ARMOR_ABSORB, nowSec: () => _frameNowMs / 1000, nowMs: () => _frameNowMs }, set: { heroHp: v  |
| `mountBulletWorldHitTick` | FACET_OR_KIND | `bullet-world-hit` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. | const _grenadePhysicsTick = mountGrenadePhysicsTick({ actions: { spawnParticles: (u, y, v) => { if (Math.random() < 0.55) _spawnParticles(u, y, v, 1, "white", 0.8, 0.35); }, removeMesh: m => scene.remove(m), deploySmokeZone: (u, v) => _deploySmokeZone(u, v), explodeGrenade: g => _explodeGrenade(g), playSfx: (str, vol) => playSfx(str, vol) } }); const _grenad |
| `mountBurnTick` | DONE | `burn` |  | Keep as native; only add stronger parity tests if behavior is suspect. | let _vignetteAmt = 0; // spring [0..1]: driven by hit impulses + low-HP pulse let _heroFireT = 0; let _heroFireDmgT = 0; const _burnTick = mountBurnTick({ get: { heroFireT: () => _heroFireT, heroFireDmgT: () => _heroFireDmgT, heroHp: () => heroHp, heroDead: () => _heroDead, heroPos: () => world.players.get("hero") }, set: { heroFireT: v => { _heroFireT = v;  |
| `mountCamDistTick` | UNHOSTED | `cam-dist` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | const _coinDropTick = mountCoinDropTick({ actions: { removeMesh: mesh => scene.remove(mesh), addScore: v => { score += v; if (typeof EventBus !== "undefined") EventBus.emit(EventBus.EVENTS.SCORE_CHANGED, { score, delta: v, source: "coin" }); }, playSfx: (str, vol) => playSfx(str, vol), spawnDamageNumber: (u, y, v, t, c) => _spawnDamageNumber(u, y, v, t, c) } |
| `mountCamPitchSprings` | HOSTED_BIND_ONLY | `cam-pitch-springs` | `data/legacy/cam-pitch-springs.json` | Add a semantic bridge test first; then build native if the behavior is small. | const _jumpGravityTick = mountJumpGravityTick({ jumpV: JUMP_V, gravity: GRAVITY, get: { velocityY: () => velocityY, spaceWasDown: () => _spaceWasDown, canDoubleJump: () => _canDoubleJump, stamina: () => _stamina }, set: { velocityY: v => { velocityY = v; }, spaceWasDown: v => { _spaceWasDown = v; }, canDoubleJump: v => { _canDoubleJump = v; }, stamina: v =>  |
| `mountCamShakeTick` | HOSTED_BIND_ONLY | `cam-shake` | `data/legacy/cam-shake.json` | Add a semantic bridge test first; then build native if the behavior is small. | const _skyDayNightTick = mountSkyDayNightTick({ actions: { setTopColor: (r,g,b) => skyUniforms.topColor.value.setRGB(r,g,b), setBottomColor: (r,g,b) => skyUniforms.bottomColor.value.setRGB(r,g,b), setFogColor: (r,g,b) => scene.fog.color.setRGB(r,g,b), setSunPos: (x,y,z) => sun.position.set(x,y,z), setSunIntensity: v => { sun.intensity = v; } } }); const _fpG |
| `mountCamVectors` | UNHOSTED | `cam-vectors` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | const _legacyPickupTick = mountLegacyPickupTick({ actions: { collectPickup: () => Bridge.collectPickup(world, "hero", pickups, 1.2), getMesh: id => pickupMeshes.get(id) \|\| null, onCollected: id => { score++; const _m = pickupMeshes.get(id); if (_m) scene.remove(_m); showToast(`Coin collected (${score}/${pickups.length})`, "success", 1500); _quest.completeQue |
| `mountCameraPosTick` | UNHOSTED | `camera-pos` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | const _optionalSystemsTick = mountOptionalSystemsTick({ actions: { getTriggerZones: () => typeof TriggerZones !== "undefined" ? TriggerZones : null, getSoundZones: () => typeof SoundZones !== "undefined" ? SoundZones : null, getCutscene: () => typeof Cutscene !== "undefined" ? Cutscene : null, getAchievements: () => typeof Achievements !== "undefined" ? Achi |
| `mountCameraZoneTick` | UNHOSTED | `camera-zone` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | const _weaponPickupTick = mountWeaponPickupTick({ get: { currentWeaponId: () => currentWeaponId, currentMag: () => pistolAmmo, currentWeaponAmmoItem: () => getWeapon().ammoItem \|\| "pistol_9mm" }, set: { currentWeaponId: v => { currentWeaponId = v; }, currentMag: v => { pistolAmmo = v; }, weaponSwitchT: v => { _weaponSwitchT = v; } }, actions: { removeMesh: m |
| `mountCanvasPrimaryAction` | UNHOSTED | `canvas-primary-action` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. |  }); } mountCanvasPrimaryAction({ renderer, get: { buildMode: () => buildMode, worldBuilder: () => worldBuilder, pointerLocked: () => pointerLocked, builderMultiList: () => window._builderMultiList, mouseMode: () => mouseMode, activeMouseScreen: () => activeMouseScreen, mouseModeCursor: () => mouseModeCursor, inventoryOpen: () => invDiv.classList.contains("o |
| `mountClockHudTick` | HOSTED_SEMANTIC_PROVEN | `clock-hud` | `data/legacy/clock-hud.json` | Build native facet, add native parity test, then flip authority. | let _fpsFrames = 0, _fpsDisplay = 0, _fpsWindowT = performance.now(); const _fpsTick = mountFpsTick({ get: { fpsFrames: () => _fpsFrames, fpsWindowT: () => _fpsWindowT, fpsDisplay: () => _fpsDisplay }, set: { fpsFrames: v => { _fpsFrames = v; }, fpsWindowT: v => { _fpsWindowT = v; }, fpsDisplay: v => { _fpsDisplay = v; } }, actions: { onNewFps: fps => { if ( |
| `mountCoinDropTick` | FACET_OR_KIND | `coin-drop` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. | const _poisonPuddleTick = mountPoisonPuddleTick({ actions: { removeMesh: mesh => scene.remove(mesh), applyPoison: () => { if (typeof StatusEffects !== "undefined") StatusEffects.apply("hero", "poison"); } } }); const _smokeZoneTick = mountSmokeZoneTick({ actions: { spawnSmoke: (u, y, v) => _spawnParticles(u, y, v, 1, "white", 0.6, 0.9) } }); const _grenadeCr |
| `mountCombatAmbientTick` | UNHOSTED | `combat-ambient` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | const _bossBarTick = mountBossBarTick(); const _comboHudTick = mountComboHudTick(); const _vehicleDashTick = mountVehicleDashTick(); let _combatAmbT = 0; const _combatAmbientTick = mountCombatAmbientTick({ get: { ambT: () => _combatAmbT }, set: { ambT: v => { _combatAmbT = v; } }, actions: { isAmbientReady: () => Sfx.isAmbientReady(), setAmbient: (k, f, t, v |
| `mountCombatHudTick` | FACET_OR_KIND | `combat-hud` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. | let _hpGhost = 100; // lagging HP ghost bar — catches up slowly after damage let _fpsFrames = 0, _fpsDisplay = 0, _fpsWindowT = performance.now(); const _fpsTick = mountFpsTick({ get: { fpsFrames: () => _fpsFrames, fpsWindowT: () => _fpsWindowT, fpsDisplay: () => _fpsDisplay }, set: { fpsFrames: v => { _fpsFrames = v; }, fpsWindowT: v => { _fpsWindowT = v; } |
| `mountComboAnnouncer` | UNHOSTED | `combo-announcer` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | let _comboCount = 0; let _comboLastT = 0; let _comboAnnouncedMul = 0; const _comboAnnouncer = mountComboAnnouncer({ DECAY: _COMBO_DECAY, get: { comboCount: () => _comboCount, comboLastT: () => _comboLastT, comboAnnouncedMul: () => _comboAnnouncedMul }, set: { comboCount: v => { _comboCount = v; }, comboAnnouncedMul: v => { _comboAnnouncedMul = v; } }, action |
| `mountComboHudTick` | FACET_OR_KIND | `combo-hud` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. | const _clockHudTick = mountClockHudTick({ actions: { getDayNightHour: () => (typeof DayNight !== "undefined") ? DayNight.getHour() : null } }); const _vignetteTick = mountVignetteTick({ get: { vignetteAmt: () => _vignetteAmt }, set: { vignetteAmt: v => { _vignetteAmt = v; } } }); const _statBarsTick = mountStatBarsTick({ get: { hpGhost: () => _hpGhost }, set |
| `mountComputerMesh` | UNHOSTED | `computer-mesh` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | const { group: enemyGroup, hpFg: enemyHpFg } = enemyMeshes.get(enemies[0].id) \|\| {}; const { compGroup, screenFront } = mountComputerMesh({ THREE, scene, computerEntity }); const worldScreens = new Map(); |
| `mountComputerUI` | UNHOSTED | `computer-ui` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | No cloned mount file found; inspect game.html directly. |
| `mountConfigEditor` | UNHOSTED | `config-editor` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | const heroAnim = createHeroAnimSystem(THREE, heroGroup); const cfgEditor = mountConfigEditor(window.GameConfig \|\| {}); const _renderSceneHierarchy = mountSceneHierarchy({ getWorldBuilder: () => worldBuilder, scene }); |
| `mountCrateSystem` | FACET_OR_KIND | `crate` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. |  document.getElementById("invWeight").textContent = Inv.totalWeight(heroInv).toFixed(1); } const { makeCrate: _makeCrate, breakCrate: _breakCrate, crates } = mountCrateSystem({ THREE, scene, get: { score: () => score, weapon: () => getWeapon(), }, set: { score: (v) => { score = v; }, }, actions: { |
| `mountCrosshairTick` | UNHOSTED | `crosshair` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | }); let _hpGhost = 100; // lagging HP ghost bar — catches up slowly after damage let _fpsFrames = 0, _fpsDisplay = 0, _fpsWindowT = performance.now(); const _fpsTick = mountFpsTick({ get: { fpsFrames: () => _fpsFrames, fpsWindowT: () => _fpsWindowT, fpsDisplay: () => _fpsDisplay }, set: { fpsFrames: v => { _fpsFrames = v; }, fpsWindowT: v => { _fpsWindowT =  |
| `mountCrouchSpeedTick` | HOSTED_BIND_ONLY | `crouch-speed` | `data/legacy/crouch-speed.json` | Add a semantic bridge test first; then build native if the behavior is small. | let _grenadePressT = 0; // performance.now() when G was pressed (0 = not held) let _heroKbU = 0, _heroKbV = 0, _heroKbT = 0; // knockback burst from heavy/boss melee let _enFsT = 0; // global enemy-footstep timer — plays for nearest approaching enemy let _heroShotAlertU = 0, _heroShotAlertV = 0, _heroShotAlertT = 0; // last shot pos alert to nearby enemies l |
| `mountDamageFeedback` | UNHOSTED | `damage-feedback` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | }); let _heroEmpT = 0; // seconds sprinting is disabled (robot EMP burst debuff) let _camShakeAmt = 0; const { flashDamage, applyScreenShake: _applyScreenShake } = mountDamageFeedback({ get: { heroHp: () => heroHp, nearDeathFired: () => _nearDeathFired, bulletTimeLeft: () => _bulletTimeLeft, hitPunchPitch: () => _hitPunchPitch, camShakeAmt: () => _camShakeAm |
| `mountDebugHudTick` | FACET_OR_KIND | `debug-hud` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. | const _cameraPosTick = mountCameraPosTick({ vectors: { camTarget: _camTarget, camOff: _camOff, camLook: _camLook, camAimTarget: _camAimTarget, camBuildLook: _camBuildLook }, actions: { getCamera: () => camera } }); const _proximityTick = mountProximityTick({ get: { dialogOpen: () => _npcDialog.isOpen, computerOpen: () => computerOpen }, set: { nearComputer:  |
| `mountDecalSystem` | FACET_OR_KIND | `decal` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. | const { spawnDecal: _spawnDecal, spawnWallScorch: _spawnWallScorch, wallScorches: _wallScorches } = mountDecalSystem({ THREE, scene }); |
| `mountDevConsoleGame` | UNHOSTED | `dev-console-game` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | window.Engine = window.Engine \|\| {}; window.Engine.debug = window.Engine.debug \|\| { godMode: false, noclip: false }; mountDevConsoleGame({ toggleGodMode: () => { window.Engine.debug.godMode = !window.Engine.debug.godMode; }, isGodMode: () => window.Engine.debug.godMode, toggleNoclip: () => { window.Engine.debug.noclip = !window.Engine.debug.noclip; }, isNocl |
| `mountDeviceBusTick` | UNHOSTED | `device-bus` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | const _cameraZoneTick = mountCameraZoneTick({ get: { camDist: () => camDist, aimAmt: () => aimAmt, camDistMax: () => CAM_DIST_MAX }, actions: { evaluateSpine: (zoom, max) => (window.CameraSpine && window.CameraSpine.evaluate) ? window.CameraSpine.evaluate(zoom, { camDistMax: max }) : null, isActiveDrone: (inCar) => !!(inCar ? _activeVehicleDef : null) && (_a |
| `mountDeviceGraphWiring` | UNHOSTED | `device-graph` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | let jumbotronScreen = null; let skyScreen = null; const { deviceBus } = mountDeviceGraphWiring({ THREE, scene, devicesApi: window.Devices \|\| null, wiresApi: window.Wires \|\| null, screenMesh: window.ScreenMesh \|\| null, worldData: WD, computerEntity, worldScreens, }); |
| `mountDifficultySelect` | UNHOSTED | `difficulty-select` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. |  }); } mountDifficultySelect({ set: { diffHpMul: (v) => { _diffHpMul = v; }, diffDmgMul: (v) => { _diffDmgMul = v; }, diffLabel: (v) => { _diffLabel = v; }, }, showToast, actions: { requestGameplayPointer, }, |
| `mountDodgeTick` | HOSTED_BIND_ONLY | `dodge` | `data/legacy/dodge.json` | Add a semantic bridge test first; then build native if the behavior is small. | const _walkAnimTick = mountWalkAnimTick({ get: { gunKickZ: () => _gunKickZ, gunReloadX: () => _gunReloadX }, set: { gunKickZ: v => { _gunKickZ = v; }, gunReloadX: v => { _gunReloadX = v; } }, actions: { walkCycle: (dt, gs) => Bridge.walkCyclePhase(walkState, dt, gs), setThighs: m => { thighL.rotation.x = +m; thighR.rotation.x = -m; }, setShins: m => { shinL. |
| `mountDropSpawner` | DONE | `drop` |  | Keep as native; only add stronger parity tests if behavior is suspect. |  spawnHealthPickup: _spawnHealthPickup, spawnArmorShard: _spawnArmorShard, spawnCoinDrop: _spawnCoinDrop, ammoPickups, weaponPickups, healthPickups, armorShards, coinDrops, } = mountDropSpawner({ THREE, scene, CFG, get: { weapon: () => getWeapon() } }); const { grenadeCrates, armorPickups } = mountStaticSupply({ THREE, scene }); |
| `mountDuelMode` | UNHOSTED | `duel-mode` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | No cloned mount file found; inspect game.html directly. |
| `mountEcsBootstrap` | UNHOSTED | `ecs-bootstrap` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | }); mountEcsBootstrap({ Core, CFG, combatSystem, createWaveSystem, pickupSystem, spawnPickup, createShopSystem, createPerkSystem, createAgentDispatch, createStatusEffectSystem, regenSystem, createWeaponSystem, createScoreSystem, createAIMovementSystem, createInventorySystem, invAdd, invRemove, invCount, invHas, createStaminaSystem, createGrenadeSystem, creat |
| `mountEnemyAiScaffoldTick` | FACET_OR_KIND | `enemy-ai-scaffold` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. | const _fastChargeTick = mountEnemyFastChargeTick({ getEnemyPos: id => world.players.get(id), resolveMove: (mover, du, dv) => GTAPhysics.resolveAABBMove(mover, du, dv, buildingBlockers), setEnemyPos: (id, x, z, rot, u, v) => world.setPlayer(id, x, z, rot, u, v), actions: { playSfx, spawnParticles: _spawnParticles } }); const _sniperTick = mountEnemySniperTick |
| `mountEnemyBossRockTick` | FACET_OR_KIND | `enemy-boss-rock` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. | const _enemyRegenTick = mountEnemyRegenTick({ actions: { getPos: id => world.players.get(id), spawnDamageNumber: (u,y,v,t,c) => _spawnDamageNumber(u,y,v,t,c) } }); const _enemySepTick = mountEnemySepTick({ actions: { getPos: id => world.players.get(id), setPos: (id,x,y,z,u,v) => world.setPlayer(id,x,y,z,u,v) } }); const _empTick = mountEnemyRobotEmpTick({ TH |
| `mountEnemyBossSlamTick` | FACET_OR_KIND | `enemy-boss-slam` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. | const _heavyGrenadeTick = mountEnemyHeavyGrenadeTick({ THREE, scene, warnRingGeo: _vfxWarnGeo, warnRingMat: _vfxWarnMat, grenades: grenades3D, GRAVITY, actions: { playSfx } }); const _bossRockTick = mountEnemyBossRockTick({ THREE, scene, warnRingGeo: _vfxWarnGeo, warnRingMat: _vfxWarnMat, grenades: grenades3D, GRAVITY, actions: { playSfx, screenShake: _apply |
| `mountEnemyBulletTick` | FACET_OR_KIND | `enemy-bullet` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. | const _grenadeArcTick = mountGrenadeArcTick({ THREE, get: { heroHp: () => heroHp }, set: { heroHp: v => { heroHp = v; }, grenadeCount: v => { grenadeCount = v; }, grenadePressT: v => { _grenadePressT = v; } }, actions: { addToScene: m => scene.add(m), flashDamage: () => flashDamage(), applyScreenShake: amt => _applyScreenShake(amt), spawnParticles: (u, y, v, |
| `mountEnemyFastChargeTick` | FACET_OR_KIND | `enemy-fast-charge` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. | const _poisonerSpitTick = mountEnemyPoisonerSpitTick({ THREE, scene, warnRingGeo: _vfxWarnGeo, grenades: grenades3D, GRAVITY, actions: { playSfx } }); const _incendiaryTick = mountEnemyIncendiaryTick({ THREE, scene, warnRingGeo: _vfxWarnGeo, grenades: grenades3D, GRAVITY, actions: { playSfx } }); const _bossSlamTick = mountEnemyBossSlamTick({ ARMOR_ABSORB, g |
| `mountEnemyFootstepTick` | FACET_OR_KIND | `enemy-footstep` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. | const _waveHudTick = mountWaveHudTick({ get: { waveBeepLast: () => _waveBeepLast, lastWaveNum: () => _lastWaveNum, waveBannerUntil: () => _waveBannerUntil, waveChallenge: () => _waveChallenge, waveChallengeStart: () => _waveChallengeStart, waveChallengeHits: () => _waveChallengeHits, heroLevel: () => _heroLevel, levelThresholds: () => _LEVEL_THRESHOLDS, enem |
| `mountEnemyHeavyGrenadeTick` | FACET_OR_KIND | `enemy-heavy-grenade` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. | const _dodgeTick = mountDodgeTick({ get: { dodgeCooldown: () => _dodgeCooldown, dodgeT: () => _dodgeT, dodgeVelU: () => _dodgeVelU, dodgeVelV: () => _dodgeVelV, dodgeBashDone: () => _dodgeBashDone }, set: { dodgeCooldown: v => { _dodgeCooldown = v; }, dodgeT: v => { _dodgeT = v; }, dodgeBashDone: v => { _dodgeBashDone = v; } }, actions: { getPos: () => world |
| `mountEnemyIncendiaryTick` | FACET_OR_KIND | `enemy-incendiary` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. | const _empTick = mountEnemyRobotEmpTick({ THREE, scene, shockwaves: _shockwaves, actions: { playSfx, setHeroEmpT: v => { _heroEmpT=v; }, showToast, flashDamage: o => { damageFlashEl.style.opacity=String(o); } } }); const _heavyGrenadeTick = mountEnemyHeavyGrenadeTick({ THREE, scene, warnRingGeo: _vfxWarnGeo, warnRingMat: _vfxWarnMat, grenades: grenades3D, GR |
| `mountEnemyMeshFactory` | FACET_OR_KIND | `enemy-mesh-factory` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. | resetGameState = _resetSys.resetGameState; window.resetGameState = resetGameState; const { makeEnemyMesh: _makeEnemyMesh } = mountEnemyMeshFactory({ THREE, scene }); for (const e of enemies) enemyMeshes.set(e.id, _makeEnemyMesh(e)); const { group: enemyGroup, hpFg: enemyHpFg } = enemyMeshes.get(enemies[0].id) \|\| {}; |
| `mountEnemyMeshTick` | FACET_OR_KIND | `enemy-mesh` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. | const _proximityTick = mountProximityTick({ get: { dialogOpen: () => _npcDialog.isOpen, computerOpen: () => computerOpen }, set: { nearComputer: v => { nearComputer = v; }, nearNpc: n => { _nearNpc = n; } }, actions: { getComputerPos: () => computerEntity, getScreenFront: () => screenFront \|\| null, getNpcDefs: () => NPC_DEFS, getNpcPos: id => world.players.g |
| `mountEnemyPoisonerRangedSpitTick` | FACET_OR_KIND | `enemy-poisoner-ranged-spit` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. | const _bossRockTick = mountEnemyBossRockTick({ THREE, scene, warnRingGeo: _vfxWarnGeo, warnRingMat: _vfxWarnMat, grenades: grenades3D, GRAVITY, actions: { playSfx, screenShake: _applyScreenShake } }); const _poisonerSpitTick = mountEnemyPoisonerSpitTick({ THREE, scene, warnRingGeo: _vfxWarnGeo, grenades: grenades3D, GRAVITY, actions: { playSfx } }); const _i |
| `mountEnemyPoisonerSpitTick` | FACET_OR_KIND | `enemy-poisoner-spit` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. | const _enemySepTick = mountEnemySepTick({ actions: { getPos: id => world.players.get(id), setPos: (id,x,y,z,u,v) => world.setPlayer(id,x,y,z,u,v) } }); const _empTick = mountEnemyRobotEmpTick({ THREE, scene, shockwaves: _shockwaves, actions: { playSfx, setHeroEmpT: v => { _heroEmpT=v; }, showToast, flashDamage: o => { damageFlashEl.style.opacity=String(o); } |
| `mountEnemyRegenTick` | FACET_OR_KIND | `enemy-regen` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. | const _heroFaceTick = mountHeroFaceTick({ get: { rotY: () => heroGroup.rotation.y }, set: { rotY: v => { heroGroup.rotation.y = v; } } }); const _camShakeTick = mountCamShakeTick({ get: { camShakeAmt: () => _camShakeAmt }, set: { camShakeAmt: v => { _camShakeAmt = v; } }, actions: { offsetCamera: (dx, dy) => { camera.position.x += dx; camera.position.y += dy |
| `mountEnemyRobotEmpTick` | FACET_OR_KIND | `enemy-robot-emp` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. | const _heroKbTick = mountHeroKnockbackTick({ get: { kbT: () => _heroKbT, kbU: () => _heroKbU, kbV: () => _heroKbV }, set: { kbT: v => { _heroKbT = v; }, kbU: v => { _heroKbU = v; }, kbV: v => { _heroKbV = v; } }, actions: { getPos: () => world.players.get("hero"), setPos: (x,y,z,u,v) => world.setPlayer("hero",x,y,z,u,v), resolveMove: (mover,du,dv) => GTAPhys |
| `mountEnemyRobotPlasmaTick` | FACET_OR_KIND | `enemy-robot-plasma` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. | const _bossSlamTick = mountEnemyBossSlamTick({ ARMOR_ABSORB, getHeroPos: () => world.players.get("hero"), getEnemyPos: id => world.players.get(id), enemies, actions: { playSfx, screenShake: _applyScreenShake, spawnParticles: _spawnParticles, spawnShockwave: _spawnShockwave, flashDamage, setHeroHp: v => { heroHp = v; }, setHeroArmor: v => { heroArmor = v; },  |
| `mountEnemySepTick` | FACET_OR_KIND | `enemy-sep` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. | const _camShakeTick = mountCamShakeTick({ get: { camShakeAmt: () => _camShakeAmt }, set: { camShakeAmt: v => { _camShakeAmt = v; } }, actions: { offsetCamera: (dx, dy) => { camera.position.x += dx; camera.position.y += dy; } } }); const _heroKbTick = mountHeroKnockbackTick({ get: { kbT: () => _heroKbT, kbU: () => _heroKbU, kbV: () => _heroKbV }, set: { kbT:  |
| `mountEnemySniperTick` | FACET_OR_KIND | `enemy-sniper` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. | const _incendiaryTick = mountEnemyIncendiaryTick({ THREE, scene, warnRingGeo: _vfxWarnGeo, grenades: grenades3D, GRAVITY, actions: { playSfx } }); const _bossSlamTick = mountEnemyBossSlamTick({ ARMOR_ABSORB, getHeroPos: () => world.players.get("hero"), getEnemyPos: id => world.players.get(id), enemies, actions: { playSfx, screenShake: _applyScreenShake, spaw |
| `mountEnemyStrafeMeleeTick` | FACET_OR_KIND | `enemy-strafe-melee` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. | const _poisonerRangedSpitTick = mountEnemyPoisonerRangedSpitTick({ THREE, scene, enemyBullets, actions: { playSfx } }); const _fastChargeTick = mountEnemyFastChargeTick({ getEnemyPos: id => world.players.get(id), resolveMove: (mover, du, dv) => GTAPhysics.resolveAABBMove(mover, du, dv, buildingBlockers), setEnemyPos: (id, x, z, rot, u, v) => world.setPlayer( |
| `mountEngineRegistry` | UNHOSTED | `engine-registry` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | mountNavAndAchievements({ WD }); mountEngineRegistry(); const { renderer, camera } = mountRenderer({ THREE, getComposer: () => composer }); |
| `mountEntityHooks` | UNHOSTED | `entity-hooks` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. |  resetGameState: () => resetGameState(), }); mountEntityHooks({ ARMOR_ABSORB, get: { heroHp: () => heroHp, heroArmor: () => heroArmor, heroDead: () => _heroDead, HERO_MAX_HP: () => HERO_MAX_HP, perkMaxHpBonus: () => _perkMaxHpBonus, dodgeT: () => _dodgeT, }, |
| `mountEnvironment` | UNHOSTED | `environment` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | const { skyboxPresets: _skyboxPresets } = mountSkybox({ THREE, scene, ambLight: _ambLight, sun, showToast }); const { skyUniforms, ground, sky, grid } = mountEnvironment({ THREE, scene, buildings }); const { heroGroup, torso, thighL, shinL, thighR, shinR, armL, armR, walkState, shadowBlob: _shadowBlob } = mountHeroMesh({ THREE, scene }); |
| `mountFirePatchTick` | UNHOSTED | `fire-patch` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | const _bulletWorldHitTick = mountBulletWorldHitTick({ getMp: () => _mp, barrels, crates, buildingBlockers, actions: { spawnParticles: _spawnParticles, spawnDamageNumber: _spawnDamageNumber, playSfx, explodeBarrel: (u, v) => _explodeBarrel(u, v), breakCrate: cr => _breakCrate(cr), spawnWallScorch: (u, y, v, nU, nV) => _spawnWallScorch(u, y, v, nU, nV) } }); c |
| `mountFirstLaunch` | UNHOSTED | `first-launch` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | } requestAnimationFrame(tick); mountFirstLaunch({ getFirstLaunch: () => _firstLaunch, finishComputerEntry: () => finishComputerEntry(), getApps: () => APPS, }); |
| `mountFlashlight` | UNHOSTED | `flashlight` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | const { flashLight: _flashLight, flashTarget: _flashTarget, tick: _flashlightTick } = mountFlashlight({ THREE, scene, getCamera: () => camera }); let _flashlightOn = false; |
| `mountFootstepSound` | DONE | `footstep` |  | Keep as native; only add stronger parity tests if behavior is suspect. | let _sprintFovAmt = 0; // spring [0..1]: pushes FOV wider while sprinting let _moveSpread = 0; // spring: accuracy penalty while moving/sprinting let _hitPunchPitch = 0; // spring: upward camera kick when taking damage let _footstepT = 0; // seconds until next footstep tick const _footstepSnd = mountFootstepSound({ get: { footstepT: () => _footstepT }, set:  |
| `mountFpGunPosTick` | UNHOSTED | `fp-gun-pos` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | const _vehicleDashTick = mountVehicleDashTick(); let _combatAmbT = 0; const _combatAmbientTick = mountCombatAmbientTick({ get: { ambT: () => _combatAmbT }, set: { ambT: v => { _combatAmbT = v; } }, actions: { isAmbientReady: () => Sfx.isAmbientReady(), setAmbient: (k, f, t, v, fade) => _setAmbient(k, f, t, v, fade) } }); const _skyDayNightTick = mountSkyDayN |
| `mountFpsTick` | DONE | `fps` |  | Keep as native; only add stronger parity tests if behavior is suspect. |  actions: { playSfx }, }); let _hpGhost = 100; // lagging HP ghost bar — catches up slowly after damage let _fpsFrames = 0, _fpsDisplay = 0, _fpsWindowT = performance.now(); const _fpsTick = mountFpsTick({ get: { fpsFrames: () => _fpsFrames, fpsWindowT: () => _fpsWindowT, fpsDisplay: () => _fpsDisplay }, set: { fpsFrames: v => { _fpsFrames = v; }, fpsWindowT |
| `mountFreecamTick` | HOSTED_BIND_ONLY | `freecam` | `data/legacy/freecam.json` | Add a semantic bridge test first; then build native if the behavior is small. | const _strafeMeleeTick = mountEnemyStrafeMeleeTick({ ARMOR_ABSORB, getHeroPos: () => world.players.get("hero"), getEnemyPos: id => world.players.get(id), resolveMove: (mover, du, dv) => GTAPhysics.resolveAABBMove(mover, du, dv, buildingBlockers), setEnemyPos: (id, x, z, rot, u, v) => world.setPlayer(id, x, z, rot, u, v), applyStatusEffect: effect => { if (ty |
| `mountGadgetSystem` | UNHOSTED | `gadget` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | jumbotronScreen = _worldScreens.jumbotronScreen; skyScreen = _worldScreens.skyScreen; const gadget = mountGadgetSystem({ THREE, scene, world, enemies, bullets3D, grenades3D, smokeZones, HERO_MAX_ARMOR, ARMOR_ABSORB, coinByType: _COIN_BY_TYPE, weaponDropMap: _WEAPON_DROP_MAP, get: { grenadeCount: () => grenadeCount, smokeGrenadeCount: () => smokeGrenadeCount, |
| `mountGameReset` | UNHOSTED | `game-reset` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | const enemyMeshes = new Map(); // enemy.id → { group, hpFg } const _resetSys = mountGameReset({ scene, world, Inv, CFG, enemies, enemyMeshes, bullets3D, enemyBullets, grenades3D, smokeZones, firePatches, poisonPuddles, wallScorches: _wallScorches, armorShards, speedOrbs: _speedOrbs, weaponPickups, heroInv, weaponAmmo, actions: { gadgetClearAll: () => gadget. |
| `mountGrenadeArcTick` | UNHOSTED | `grenade-arc` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | const _vehiclePhysicsTick = mountVehiclePhysicsTick({ actions: { key: k => !!keys[k], getCamYaw: () => camYaw, getPos: id => world.players.get(id) \|\| null, setPos: (id, x, yy, z, u, vv) => world.setPlayer(id, x, yy, z, u, vv), carPhysicsStep: (vId, vSt, throttle, steerIn, dt, opts) => Bridge.carPhysicsStep(world, vId, vSt, throttle, steerIn, dt, opts), updat |
| `mountGrenadeCrateTick` | FACET_OR_KIND | `grenade-crate` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. | const _armorShardTick = mountArmorShardTick({ get: { heroArmor: () => heroArmor, maxArmor: () => HERO_MAX_ARMOR }, set: { heroArmor: v => { heroArmor = v; } }, actions: { removeMesh: mesh => scene.remove(mesh), playSfx: (str, vol) => playSfx(str, vol), spawnDamageNumber: (u, y, v, t, c) => _spawnDamageNumber(u, y, v, t, c), showToast: (msg, type, dur) => sho |
| `mountGrenadePhysicsTick` | UNHOSTED | `grenade-physics` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | const _ammoReloadTick = mountAmmoReloadTick({ set: { reloading: v => { _reloading = v; }, pistolAmmo: v => { pistolAmmo = v; }, pistolCooldown: v => { pistolCooldown = v; }, getPistolAmmo: () => pistolAmmo }, actions: { getReloadDur: () => RELOAD_DUR(), getMagCap: () => PISTOL_MAG_CAP(), getWeapon: () => getWeapon(), countInvAmmo: item => Inv.countItem(heroI |
| `mountGrenadeWarnTick` | UNHOSTED | `grenade-warn` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | const _crouchSpeedTick = mountCrouchSpeedTick({ actions: { spawnSprintTrail: () => { const _spTp = world.players.get("hero"); if (_spTp) _spawnParticles(_spTp.u, _spTp.y + 0.08, _spTp.v, 1, "orange", 1.2, 0.22); } } }); const _waveHudTick = mountWaveHudTick({ get: { waveBeepLast: () => _waveBeepLast, lastWaveNum: () => _lastWaveNum, waveBannerUntil: () => _w |
| `mountHazardZones` | FACET_OR_KIND | `hazard-zones` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. | const { spawnFirePatch: _spawnFirePatch, spawnPoisonPuddle: _spawnPoisonPuddle, firePatches, poisonPuddles, } = mountHazardZones({ THREE, scene }); const { pickupMeshes } = mountPickupMeshes({ THREE, scene, pickups }); |
| `mountHealthPickupTick` | FACET_OR_KIND | `health-pickup` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. | const _heroRegenTick = mountHeroRegenTick({ get: { heroHp: () => heroHp, maxHp: () => HERO_MAX_HP, perkMaxHpBonus: () => _perkMaxHpBonus, lastDamageT: () => heroLastDamageT, regenDelay: () => HERO_REGEN_DELAY, regenRate: () => HERO_REGEN_RATE, perkRegenBonus: () => _perkRegenBonus }, set: { heroHp: v => { heroHp = v; }, nearDeathFired: v => { _nearDeathFired |
| `mountHeartbeat` | DONE | `heartbeat` |  | Keep as native; only add stronger parity tests if behavior is suspect. | let _bulletTimeLeft = 0; // seconds remaining of bullet-time slow (sniper incoming / headshot kill / wave clear) let _nearDeathFired = false; // near-death bullet-time has fired this life (resets above 15 HP) let _heartbeatT = 0; const _heartbeat = mountHeartbeat({ get: { heroHp: () => heroHp, HERO_MAX_HP: () => HERO_MAX_HP, heroDead: () => _heroDead, heartb |
| `mountHeroFaceTick` | HOSTED_BIND_ONLY | `hero-face` | `data/legacy/hero-face.json` | Add a semantic bridge test first; then build native if the behavior is small. | const _combatAmbientTick = mountCombatAmbientTick({ get: { ambT: () => _combatAmbT }, set: { ambT: v => { _combatAmbT = v; } }, actions: { isAmbientReady: () => Sfx.isAmbientReady(), setAmbient: (k, f, t, v, fade) => _setAmbient(k, f, t, v, fade) } }); const _skyDayNightTick = mountSkyDayNightTick({ actions: { setTopColor: (r,g,b) => skyUniforms.topColor.val |
| `mountHeroInventory` | FACET_OR_KIND | `hero-inventory` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. | const CFG = window.GameConfig \|\| {}; const { heroInv, heroHealth } = mountHeroInventory({ Inv, Health, CFG }); const world = new WorldState(1); world.setPlayer("hero", 0, 0, 0, 0, 0); |
| `mountHeroKnockbackTick` | HOSTED_BIND_ONLY | `hero-knockback` | `data/legacy/hero-knockback.json` | Add a semantic bridge test first; then build native if the behavior is small. | const _fpGunPosTick = mountFpGunPosTick({ get: { weaponSwitchT: () => _weaponSwitchT }, set: { weaponSwitchT: v => { _weaponSwitchT = v; } }, actions: { setPosition: (x,y,z) => _fpGunGroup.position.set(x,y,z), setRotation: (x,y,z) => _fpGunGroup.rotation.set(x,y,z) } }); const _walkAnimTick = mountWalkAnimTick({ get: { gunKickZ: () => _gunKickZ, gunReloadX:  |
| `mountHeroLifecycle` | FACET_OR_KIND | `hero-lifecycle` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. | const _spawnClearPos = _spawnSys.spawnClearPos; const _spawnMeshes = _spawnSys.spawnMeshes; let _heroDead = false; const _lifecycle = mountHeroLifecycle({ get: { heroDead: () => _heroDead, shotsFired: () => _shotsFired, shotsHit: () => _shotsHit, damageDealt: () => _damageDealt, enemyKills: () => enemyKills, score: () => score, perkMaxHpBonus: () => _perkMax |
| `mountHeroMesh` | FACET_OR_KIND | `hero-mesh` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. | const { skyUniforms, ground, sky, grid } = mountEnvironment({ THREE, scene, buildings }); const { heroGroup, torso, thighL, shinL, thighR, shinR, armL, armR, walkState, shadowBlob: _shadowBlob } = mountHeroMesh({ THREE, scene }); const { gunMount: _gunMount, fpGunGroup: _fpGunGroup, switchGunMesh: _switchGunMesh, registerGunMesh: _wvRegisterGunMesh } = creat |
| `mountHeroMoveTick` | FACET_OR_KIND | `hero-move` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. | const _enemyAiScaffold = mountEnemyAiScaffoldTick({ getEnemyPos: id => world.players.get(id), setEnemyPos: (id, a, b, c, u, v) => world.setPlayer(id, a, b, c, u, v), resolveMove: (mover, du, dv) => GTAPhysics.resolveAABBMove(mover, du, dv, buildingBlockers), hasLOS: (u0, v0, u1, v1) => hasLOS(u0, v0, u1, v1, buildingBlockers), makeBt: opts => typeof Behavior |
| `mountHeroRegenTick` | DONE | `hero-regen` |  | Keep as native; only add stronger parity tests if behavior is suspect. | window._loadingDismissed = false; const _platformSys = mountPlatformSystem(); const _vehicleRenderTick = mountVehicleRenderTick(); const _heroRegenTick = mountHeroRegenTick({ get: { heroHp: () => heroHp, maxHp: () => HERO_MAX_HP, perkMaxHpBonus: () => _perkMaxHpBonus, lastDamageT: () => heroLastDamageT, regenDelay: () => HERO_REGEN_DELAY, regenRate: () => HE |
| `mountHudElements` | FACET_OR_KIND | `hud-elements` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. | const { GRAVITY, JUMP_V, WALK, SPRINT } = makeMovementStats(CFG); const { dom: _dom, hud, mini, MINI_HALF, dmgDirSvg: _dmgDirSvg, dmgDirG: _dmgDirG } = mountHudElements(CFG); |
| `mountHudTemplate` | FACET_OR_KIND | `hud-template` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. | let composer = null; mountHudTemplate(); |
| `mountJumpGravityTick` | HOSTED_BIND_ONLY | `jump-gravity` | `data/legacy/jump-gravity.json` | Add a semantic bridge test first; then build native if the behavior is small. | const _npcMoveTick = mountNpcMoveTick({ actions: { getPos: id => world.players.get(id), setPos: (id,x,y,z,u,v) => world.setPlayer(id,x,y,z,u,v), wanderStep: (id,h,spd,dt2) => Bridge.wanderStep(world,id,h,spd,dt2), clampToArena: (id,r) => Bridge.clampToArena(world,id,r), toRenderPos: id => Bridge.engineToRenderPos(world.players.get(id)) } }); const _scopeFovT |
| `mountKeydownHandler` | UNHOSTED | `keydown-handler` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. |  spawnDamageNumber: _spawnDamageNumber, }, }); mountKeydownHandler({ keys, invDiv, renderInventory, getState: () => ({ buildMode, computerOpen, computerEntering, nearComputer, camYaw, camSide, CFG, currentWeaponId, pistolAmmo, weaponAmmo, _reloading, grenadeCount, inCar, activeVehicleId, vehicleStates, heroInv, heroHp, HERO_MAX_HP, _perkMaxHpBonus, enemies, |
| `mountKeyupHandler` | UNHOSTED | `keyup-handler` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. |  }, weaponDropMap: _WEAPON_DROP_MAP, coinByType: _COIN_BY_TYPE, });; mountKeyupHandler({ keys, getState: () => ({ buildMode, computerOpen, _heroDead, _grenadePressT, heroHp, world, grenadeCount, }), set: { grenadePressT: (v) => { _grenadePressT = v; }, |
| `mountKillTracking` | UNHOSTED | `kill-tracking` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | const _refreshPerkHud = _perkSys.refreshPerkHud; const _applyPerk = _perkSys.applyPerk; const _activePerkLabels = _perkSys.activePerkLabels; const _killTracking = mountKillTracking({ enemies, world, get: { perkLifesteal: () => _perkLifesteal, heroDead: () => _heroDead, heroHp: () => heroHp, HERO_MAX_HP: () => HERO_MAX_HP, perkMaxHpBonus: () => _perkMaxHpBonu |
| `mountLayerTransitionTick` | HOSTED_BIND_ONLY | `layer-transition` | `data/legacy/layer-transition.json` | Add a semantic bridge test first; then build native if the behavior is small. | const _platformSys = mountPlatformSystem(); const _vehicleRenderTick = mountVehicleRenderTick(); const _heroRegenTick = mountHeroRegenTick({ get: { heroHp: () => heroHp, maxHp: () => HERO_MAX_HP, perkMaxHpBonus: () => _perkMaxHpBonus, lastDamageT: () => heroLastDamageT, regenDelay: () => HERO_REGEN_DELAY, regenRate: () => HERO_REGEN_RATE, perkRegenBonus: ()  |
| `mountLegacyPickupTick` | UNHOSTED | `legacy-pickup` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | const _grenadeCrateTick = mountGrenadeCrateTick({ get: { grenadeCount: () => grenadeCount }, set: { grenadeCount: v => { grenadeCount = v; } }, actions: { playSfx: (str, vol) => playSfx(str, vol), showToast: (msg, type, dur) => showToast(msg, type, dur) } }); const _armorVestTick = mountArmorVestTick({ get: { heroArmor: () => heroArmor, maxArmor: () => HERO_ |
| `mountLevelSystem` | UNHOSTED | `level` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. |  }, }); const _levelSys = mountLevelSystem({ get: { heroLvlDmgMul: () => _heroLvlDmgMul, heroLvlSpeedBonus:() => _heroLvlSpeedBonus, heroExtraStaminaMax: () => _heroExtraStaminaMax, stamina: () => _stamina, STAMINA_MAX: () => STAMINA_MAX, heroHp: () => heroHp, HERO_MAX_HP: () => HERO_MAX_HP, heroPos: () => world.players.get("hero"), |
| `mountLighting` | UNHOSTED | `lighting` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. |  mountVfxInit({ THREE, scene, camera, Vfx, warnRingGeo: _vfxWarnGeo, warnRingMat: _vfxWarnMat }); const { ambLight: _ambLight, sun } = mountLighting({ THREE, scene, Engine, renderer }); if (typeof WaveManager !== "undefined") { mountWaveEvents({ |
| `mountLoadCheckOverlay` | UNHOSTED | `load-check-overlay` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. |  createGrenadeCrateSystem, createArmorVestSystem, }); mountLoadCheckOverlay({ actions: { showToast, setTimeout: (fn, ms) => setTimeout(fn, ms), setInterval: (fn, ms) => setInterval(fn, ms), clearInterval: id => clearInterval(id), }, }); mountRuntimeErrorReporter(); </script> |
| `mountLoaders` | UNHOSTED | `loaders` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | const showToast = Notifications.showToast; const _addKillFeedEntry = Notifications.addKillFeedEntry; const { Loaders } = mountLoaders(); let composer = null; |
| `mountMediaPickups` | UNHOSTED | `media-pickups` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. |  worldScreens, }); const { heroMedia, worldMedia, spawnMedia: _spawnMedia, tick: _mediaPickupsTick } = mountMediaPickups({ THREE, scene, actions: { playSfx: (str, vol) => playSfx(str, vol) } }); _spawnMedia({ id: "cd_mix", kind: "cd", label: "MIX_2026", files: { "/track1.wav": "(440Hz sine)", "/track2.wav": "(beep loop)", "/readme.txt": "Burned by jumbotron  |
| `mountMotionSprings` | HOSTED_BIND_ONLY | `motion-springs` | `data/legacy/motion-springs.json` | Add a semantic bridge test first; then build native if the behavior is small. | let _footstepT = 0; // seconds until next footstep tick const _footstepSnd = mountFootstepSound({ get: { footstepT: () => _footstepT }, set: { footstepT: v => { _footstepT = v; } }, actions: { playSfx } }); let _gunBobPhase = 0; // oscillation phase for FP weapon bob let _strafeRollAmt = 0; // spring [−1..1]: FP camera roll when strafing const _motionSprings |
| `mountMouseInput` | FACET_OR_KIND | `mouse-input` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. |  }, 50); } let pointerLocked = false; mountMouseInput({ getState: () => ({ buildMode, computerOpen, computerEntering, _heroDead, mouseMode, aiming, pointerLocked, freeCamYaw, freeCamPitch, camYaw, camPitch, camDist, CAM_DIST_MIN, CAM_DIST_MAX, CFG, currentWeaponId, pistolAmmo, _reloading, }), getRenderer: () => renderer, getWorldBuilder: () => worldBuilder, |
| `mountMpBadge` | UNHOSTED | `mp-badge` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | No cloned mount file found; inspect game.html directly. |
| `mountNavAndAchievements` | UNHOSTED | `nav-and-achievements` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | mountTriggerZoneInit({ THREE, scene, showToast }); mountNavAndAchievements({ WD }); mountEngineRegistry(); |
| `mountNpcDialog` | FACET_OR_KIND | `npc-dialog` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. | window.showToast = Notifications.showToast; // keep global API const _npcDialog = mountNpcDialog(DEFAULT_NPC_DIALOGS); const _quest = mountQuestPanel({ showToast }); window.addQuest = (...a) => _quest.addQuest(...a); window.completeQuestStep = (...a) => _quest.completeQuestStep(...a); |
| `mountNpcMeshFactory` | FACET_OR_KIND | `npc-mesh-factory` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. | const { pickupMeshes } = mountPickupMeshes({ THREE, scene, pickups }); const { npcMeshes } = mountNpcMeshFactory({ THREE, scene, npcDefs: NPC_DEFS }); const enemyMeshes = new Map(); // enemy.id → { group, hpFg } |
| `mountNpcMoveTick` | FACET_OR_KIND | `npc-move` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. | const _sniperTick = mountEnemySniperTick({ THREE, scene, enemyBullets, resolveMove: (mover, du, dv) => GTAPhysics.resolveAABBMove(mover, du, dv, buildingBlockers), getEnemyPos: id => world.players.get(id), setEnemyPos: (id, x, z, rot, u, v) => world.setPlayer(id, x, z, rot, u, v), actions: { playSfx, screenShake: _applyScreenShake, alertShot: () => { showToa |
| `mountOptionalSystemsTick` | UNHOSTED | `optional-systems` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | const _armorVestTick = mountArmorVestTick({ get: { heroArmor: () => heroArmor, maxArmor: () => HERO_MAX_ARMOR }, set: { heroArmor: v => { heroArmor = v; } }, actions: { playSfx: (str, vol) => playSfx(str, vol), showToast: (msg, type, dur) => showToast(msg, type, dur) } }); const _coinDropTick = mountCoinDropTick({ actions: { removeMesh: mesh => scene.remove( |
| `mountParticleAndTerrain` | UNHOSTED | `particle-and-terrain` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | window._setFogFar = v => { if (scene.fog) scene.fog.far = v; }; _buildArenaBoundary(scene); // deferred here — scene now exists mountParticleAndTerrain({ THREE, scene, showToast }); mountTriggerZoneInit({ THREE, scene, showToast }); |
| `mountPerkSystem` | UNHOSTED | `perk` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. |  if (_p && typeof _p.catch === "function") _p.catch(() => {}); } catch (_e) {} }, 120); } const _perkSys = mountPerkSystem({ Inv, get: { perkDmgMul: () => _perkDmgMul, perkSpeedBonus: () => _perkSpeedBonus, perkRegenBonus: () => _perkRegenBonus, perkReloadMul: () => _perkReloadMul, perkMaxHpBonus: () => _perkMaxHpBonus, |
| `mountPickupMeshes` | UNHOSTED | `pickup-meshes` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. |  firePatches, poisonPuddles, } = mountHazardZones({ THREE, scene }); const { pickupMeshes } = mountPickupMeshes({ THREE, scene, pickups }); const { npcMeshes } = mountNpcMeshFactory({ THREE, scene, npcDefs: NPC_DEFS }); |
| `mountPlatformSystem` | UNHOSTED | `platform` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. |  }, }); window._loadingDismissed = false; const _platformSys = mountPlatformSystem(); const _vehicleRenderTick = mountVehicleRenderTick(); const _heroRegenTick = mountHeroRegenTick({ get: { heroHp: () => heroHp, maxHp: () => HERO_MAX_HP, perkMaxHpBonus: () => _perkMaxHpBonus, lastDamageT: () => heroLastDamageT, regenDelay: () => HERO_REGEN_DELAY, regenRate:  |
| `mountPoisonPuddleTick` | UNHOSTED | `poison-puddle` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | const _ammoPickupTick = mountAmmoPickupTick({ actions: { removeMesh: mesh => scene.remove(mesh), addAmmo: (item, qty) => Inv.addItem(heroInv, item \|\| getWeapon().ammoItem \|\| "pistol_9mm", qty), playSfx: (str, vol) => playSfx(str, vol), spawnDamageNumber: (u, y, v, t, c) => _spawnDamageNumber(u, y, v, t, c) } }); const _healthPickupTick = mountHealthPickupTic |
| `mountPostProcessing` | UNHOSTED | `post-processing` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | const { renderer, camera } = mountRenderer({ THREE, getComposer: () => composer }); mountPostProcessing({ THREE, renderer, scene, camera, onReady: c => { composer = c; } }); |
| `mountProximityTick` | UNHOSTED | `proximity` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | const _camDistTick = mountCamDistTick({ get: { camDist: () => camDist, snapZoomTarget: () => snapZoomTarget, computerEntering: () => computerEntering, computerEntryT: () => computerEntryT, camDistBeforeEntry: () => camDistBeforeEntry, computerEntryDur: () => COMPUTER_ENTRY_DUR }, set: { camDist: v => { camDist = v; }, snapZoomTarget: v => { snapZoomTarget =  |
| `mountQuestPanel` | UNHOSTED | `quest-panel` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | const _npcDialog = mountNpcDialog(DEFAULT_NPC_DIALOGS); const _quest = mountQuestPanel({ showToast }); window.addQuest = (...a) => _quest.addQuest(...a); window.completeQuestStep = (...a) => _quest.completeQuestStep(...a); |
| `mountRenderer` | UNHOSTED | `renderer` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | const { Loaders } = mountLoaders(); let composer = null; mountHudTemplate(); mountEngineRegistry(); const { renderer, camera } = mountRenderer({ THREE, getComposer: () => composer }); mountPostProcessing({ THREE, renderer, scene, camera, onReady: c => { composer = c; } }); |
| `mountRuntimeErrorReporter` | UNHOSTED | `runtime-error-reporter` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. |  }, }); mountRuntimeErrorReporter(); </script> </body> </html> |
| `mountSaveWiring` | UNHOSTED | `save` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | const { save: _saveGame, load: _loadGame } = mountSaveWiring({ GameProgress, get: { score: () => score, enemyKills: () => enemyKills, heroHp: () => heroHp, heroMaxHp: () => HERO_MAX_HP, currentWeaponId: () => currentWeaponId, computerOpen: () => computerOpen, heroPos: () => world.getPlayer ? (world.getPlayer("hero") \|\| { u: 0, v: 0, y: 0 }) : { u: 0, v: 0, y |
| `mountScene` | UNHOSTED | `scene` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | const pickups = (WD.pickups \|\| []).map(p => Object.assign({ collected: false }, p)); let score = 0; const { scene } = mountScene({ THREE }); window._setFogNear = v => { if (scene.fog) scene.fog.near = v; }; window._setFogFar = v => { if (scene.fog) scene.fog.far = v; }; _buildArenaBoundary(scene); // deferred here — scene now exists |
| `mountSceneHierarchy` | UNHOSTED | `scene-hierarchy` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | const cfgEditor = mountConfigEditor(window.GameConfig \|\| {}); const _renderSceneHierarchy = mountSceneHierarchy({ getWorldBuilder: () => worldBuilder, scene }); const _mpState = { myIp: null, onMpWelcomeHook: null, onMpBuildEvent: null, pendingFriendRequests: [] }; let _mpRef = null; |
| `mountScopeFovTick` | HOSTED_BIND_ONLY | `scope-fov` | `data/legacy/scope-fov.json` | Add a semantic bridge test first; then build native if the behavior is small. | const _robotPlasmaTick = mountEnemyRobotPlasmaTick({ THREE, scene, enemyBullets, actions: { playSfx } }); const _strafeMeleeTick = mountEnemyStrafeMeleeTick({ ARMOR_ABSORB, getHeroPos: () => world.players.get("hero"), getEnemyPos: id => world.players.get(id), resolveMove: (mover, du, dv) => GTAPhysics.resolveAABBMove(mover, du, dv, buildingBlockers), setEnem |
| `mountScreenInteraction` | FACET_OR_KIND | `screen-interaction` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. | const _setAmbient = Sfx.setAmbient; const _screenInteraction = mountScreenInteraction({ THREE, getScreenMesh: () => window.ScreenMesh \|\| null, get: { worldScreens: () => worldScreens, camera: () => camera, mouseMode: () => mouseMode }, set: { activeMouseScreen: v => { activeMouseScreen = v; }, mouseMode: v => { mouseMode = v; }, mouseModeCursorX: v => { mous |
| `mountScreenMeshTick` | FACET_OR_KIND | `screen-mesh` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. | const { _camTarget, _camOff, _camLook, _camAimTarget, _camBuildLook } = mountCamVectors({ THREE }); const _cameraPosTick = mountCameraPosTick({ vectors: { camTarget: _camTarget, camOff: _camOff, camLook: _camLook, camAimTarget: _camAimTarget, camBuildLook: _camBuildLook }, actions: { getCamera: () => camera } }); const _proximityTick = mountProximityTick({ g |
| `mountSettingsPanel` | UNHOSTED | `settings-panel` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. |  isBlocked: () => computerOpen \|\| buildMode, }); const _settings = mountSettingsPanel({ getCFG: () => (typeof CFG !== "undefined" ? CFG : {}), getRenderer: () => renderer, getBuildMode: () => buildMode, password: "5DENGINE", initialSniperSens: 3.0, }); const grenades3D = []; let grenadeCount = 3; |
| `mountShootSystem` | DONE | `shoot` |  | Keep as native; only add stronger parity tests if behavior is suspect. | const RELOAD_DUR = () => Math.round((getWeapon().reloadDuration \|\| 1200) * _perkReloadMul); const PISTOL_MAG_CAP = () => getWeapon().magCap \|\| 12; const _shootSys = mountShootSystem({ THREE, scene, camera, world, bullets3D, heroGroup, Inv, get: { shopOpen: () => _shop.isOpen, reloading: () => _reloading, pistolAmmo: () => pistolAmmo, pistolCooldown: () => pi |
| `mountShopPanel` | UNHOSTED | `shop-panel` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. |  { id: "mines_x3", name: "Mine Kit ×3", desc: "+3 trap mines (M)", cost: 5, action: () => { _mineCount = Math.min(9, _mineCount + 3); } }, { id: "ammo_smg", name: "SMG Ammo", desc: "+40 rounds (SMG uses pistol ammo)", cost: 3, action: () => { Inv.addItem(heroInv, "pistol_9mm", 40); } }, { id: "ammo_sniper", name: "Sniper Rounds", desc: "+10 rounds (sniper us |
| `mountSkyDayNightTick` | UNHOSTED | `sky-day-night` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | const _comboHudTick = mountComboHudTick(); const _vehicleDashTick = mountVehicleDashTick(); let _combatAmbT = 0; const _combatAmbientTick = mountCombatAmbientTick({ get: { ambT: () => _combatAmbT }, set: { ambT: v => { _combatAmbT = v; } }, actions: { isAmbientReady: () => Sfx.isAmbientReady(), setAmbient: (k, f, t, v, fade) => _setAmbient(k, f, t, v, fade)  |
| `mountSkybox` | UNHOSTED | `skybox` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | }); const _applyLevelUpBuff = _levelSys.applyLevelUpBuff; const { skyboxPresets: _skyboxPresets } = mountSkybox({ THREE, scene, ambLight: _ambLight, sun, showToast }); const { skyUniforms, ground, sky, grid } = mountEnvironment({ THREE, scene, buildings }); |
| `mountSmokeZoneTick` | UNHOSTED | `smoke-zone` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | const _healthPickupTick = mountHealthPickupTick({ get: { heroHp: () => heroHp, maxHp: () => HERO_MAX_HP }, set: { heroHp: v => { heroHp = v; } }, actions: { removeMesh: mesh => scene.remove(mesh), playSfx: (str, vol) => playSfx(str, vol), spawnDamageNumber: (u, y, v, t, c) => _spawnDamageNumber(u, y, v, t, c), showToast: (msg, type, dur) => showToast(msg, ty |
| `mountSniperSway` | HOSTED_BIND_ONLY | `sniper-sway` | `data/legacy/sniper-sway.json` | Add a semantic bridge test first; then build native if the behavior is small. | let _wasSniperScope = false; let _sniperSavedCamDist = 7; // camDist restored when scope is dropped let _lastSwayPitch = 0; // sway offset applied last frame — subtracted before re-applying let _lastSwayYaw = 0; const _sniperSway = mountSniperSway({ get: { scopeSwayT: () => _scopeSwayT, breathHoldT: () => _breathHoldT, lastSwayPitch: () => _lastSwayPitch, la |
| `mountSpawnSystem` | DONE | `spawn` |  | Keep as native; only add stronger parity tests if behavior is suspect. |  (WD.entities && WD.entities.computer) \|\| { id: "pc1", u: -4, v: 4 } ); const _spawnSys = mountSpawnSystem({ THREE, getScene: () => scene, buildingBlockers }); const _spawnSys = mountSpawnSystem({ THREE, getScene: () => scene, buildingBlockers }); const _spawnPoints = _spawnSys.spawnPoints; const _addSpawnPoint = _spawnSys.addSpawnPoint; const _getSpawnPoint |
| `mountSpeedBoostTick` | DONE | `speed-boost` |  | Keep as native; only add stronger parity tests if behavior is suspect. | let _speedBoostT = 0; let _speedTrailT = 0; const _speedBoostTick = mountSpeedBoostTick({ get: { speedBoostT: () => _speedBoostT, speedTrailT: () => _speedTrailT }, set: { speedBoostT: v => { _speedBoostT = v; }, speedTrailT: v => { _speedTrailT = v; } }, actions: { spawnTrail: () => { const _h = world.players.get("hero"); if (_h) _spawnParticles(_h.u - Math |
| `mountSpeedOrbSpawner` | FACET_OR_KIND | `speed-orb` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. | let _speedBoostT = 0; let _speedTrailT = 0; const _speedBoostTick = mountSpeedBoostTick({ get: { speedBoostT: () => _speedBoostT, speedTrailT: () => _speedTrailT }, set: { speedBoostT: v => { _speedBoostT = v; }, speedTrailT: v => { _speedTrailT = v; } }, actions: { spawnTrail: () => { const _h = world.players.get("hero"); if (_h) _spawnParticles(_h.u - Math |
| `mountSpeedOrbTick` | FACET_OR_KIND | `speed-orb` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. | const _layerTransTick = mountLayerTransitionTick({ get: { layerId: () => world.layerId }, actions: { boundaryAt: (u, v, blds) => world.boundaryAt(u, v, blds), bldgName: id => _layerBldgName(id), logTransition: (from, to) => world.logTransition("hero", from, to, "phase_shift"), showToast: (msg, type, dur) => showToast(msg, type, dur), playSfx: (str, vol) => p |
| `mountStaminaTick` | DONE | `stamina` |  | Keep as native; only add stronger parity tests if behavior is suspect. | let _slideT = 0, _slideDU = 0, _slideDV = 0, _ctrlWasDown = false; // sprint slide state let _stamina = 100; // 0..100; depletes while sprinting, regens when idle let _heroExtraStaminaMax = 0; // bonus from kill level 3 const _staminaTick = mountStaminaTick({ STAMINA_DRAIN, STAMINA_REGEN, STAMINA_MAX, STAMINA_LOCKOUT, get: { stamina: () => _stamina, heroEmpT |
| `mountStarterQuests` | UNHOSTED | `starter-quests` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | window._saveGame = _saveGame; window._loadGame = _loadGame; mountStarterQuests({ addQuest: (...a) => _quest.addQuest(...a), showToast, loadGame: () => _loadGame(), }); document.addEventListener("keydown", (e) => { if (e.code === "KeyJ" && !computerOpen) _quest.renderQuests(); |
| `mountStatBarsTick` | UNHOSTED | `stat-bars` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | const _crosshairTick = mountCrosshairTick(); const _combatHudTick = mountCombatHudTick(); const _clockHudTick = mountClockHudTick({ actions: { getDayNightHour: () => (typeof DayNight !== "undefined") ? DayNight.getHour() : null } }); const _vignetteTick = mountVignetteTick({ get: { vignetteAmt: () => _vignetteAmt }, set: { vignetteAmt: v => { _vignetteAmt =  |
| `mountStaticSupply` | UNHOSTED | `static-supply` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. |  ammoPickups, weaponPickups, healthPickups, armorShards, coinDrops, } = mountDropSpawner({ THREE, scene, CFG, get: { weapon: () => getWeapon() } }); const { grenadeCrates, armorPickups } = mountStaticSupply({ THREE, scene }); let _speedBoostT = 0; |
| `mountStatusTintTick` | UNHOSTED | `status-tint` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | const _throwFlashbang = gadget.throwFlashbang; const _throwGrenade = gadget.throwGrenade; const _explodeGrenade = gadget.explodeGrenade; let _heroBlindT = 0; // seconds of white-out flash; written by gadget.explodeGrenade via set.heroBlindT const _statusTintTick = mountStatusTintTick({ get: { heroBlindT: () => _heroBlindT, heroFireT: () => _heroFireT, heroEm |
| `mountTestBridge` | UNHOSTED | `test-bridge` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | const _mp = (_mpRef = _appMp.mp); const _duelMode = _appMp.duelMode; mountTestBridge({ enemies, world, WaveManager, THREE, renderer, camera, composer, scene, Vfx, bullets3D, enemyBullets, buildingBlockers, hasLOS, CFG, shop: _shop, settings: _settings, npcDialog: _npcDialog, get: { heroHp: () => heroHp, HERO_MAX_HP: () => HERO_MAX_HP, perkMaxHpBonus: () => _ |
| `mountTriggerZoneInit` | UNHOSTED | `trigger-zone` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | mountParticleAndTerrain({ THREE, scene, showToast }); mountTriggerZoneInit({ THREE, scene, showToast }); mountNavAndAchievements({ WD }); |
| `mountVehicleDashTick` | FACET_OR_KIND | `vehicle-dash` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. | const _vignetteTick = mountVignetteTick({ get: { vignetteAmt: () => _vignetteAmt }, set: { vignetteAmt: v => { _vignetteAmt = v; } } }); const _statBarsTick = mountStatBarsTick({ get: { hpGhost: () => _hpGhost }, set: { hpGhost: v => { _hpGhost = v; } } }); const _bossBarTick = mountBossBarTick(); const _comboHudTick = mountComboHudTick(); const _vehicleDash |
| `mountVehicleMeshFactory` | FACET_OR_KIND | `vehicle-mesh-factory` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. |  } }); const { makeVehicleMesh: _makeVehicleMesh } = mountVehicleMeshFactory({ THREE }); const { vehicleMeshes, carGroup, carBody } = mountVehicleMeshes({ THREE, scene, vehicleDefs: VEHICLE_DEFS, makeVehicleMesh: _makeVehicleMesh }); |
| `mountVehicleMeshes` | FACET_OR_KIND | `vehicle-meshes` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. | const { makeVehicleMesh: _makeVehicleMesh } = mountVehicleMeshFactory({ THREE }); const { vehicleMeshes, carGroup, carBody } = mountVehicleMeshes({ THREE, scene, vehicleDefs: VEHICLE_DEFS, makeVehicleMesh: _makeVehicleMesh }); const { |
| `mountVehiclePhysicsTick` | FACET_OR_KIND | `vehicle-physics` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. | const _deviceBusTick = mountDeviceBusTick({ actions: { getDeviceBus: () => typeof deviceBus !== "undefined" ? deviceBus : null, getHeroPos: () => world.players.get("hero") \|\| { u: 0, v: 0 }, hasAudioMixer: () => typeof audioMixer !== "undefined" && !!audioMixer, playSfx: (str, vol) => playSfx(str, vol), pollMon1Bridge: () => { if (window.__mon1Bridge) window |
| `mountVehicleRenderTick` | FACET_OR_KIND | `vehicle-render` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. | }); window._loadingDismissed = false; const _platformSys = mountPlatformSystem(); const _vehicleRenderTick = mountVehicleRenderTick(); const _heroRegenTick = mountHeroRegenTick({ get: { heroHp: () => heroHp, maxHp: () => HERO_MAX_HP, perkMaxHpBonus: () => _perkMaxHpBonus, lastDamageT: () => heroLastDamageT, regenDelay: () => HERO_REGEN_DELAY, regenRate: () = |
| `mountVfxInit` | UNHOSTED | `vfx` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | const { _warnRingGeo, _warnRingMat, _spawnParticles, _ejectCasing, _spawnDamageNumber, _triggerMuzzleFlash, _spawnShockwave, _shockwaves } = mountVfxInit({ THREE, scene, camera, Vfx, warnRingGeo: _vfxWarnGeo, warnRingMat: _vfxWarnMat }); const { ambLight: _ambLight, sun } = mountLighting({ THREE, scene, Engine, renderer }); |
| `mountVictoryPlayAgain` | UNHOSTED | `victory-play-again` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | const _heroRespawn = _lifecycle.heroRespawn; const _tickDeath = _lifecycle.tickDeath; mountVictoryPlayAgain({ resetGameState: () => resetGameState() }); let resetGameState; |
| `mountVignetteTick` | HOSTED_BIND_ONLY | `vignette` | `data/legacy/vignette.json` | Add a semantic bridge test first; then build native if the behavior is small. | const _fpsTick = mountFpsTick({ get: { fpsFrames: () => _fpsFrames, fpsWindowT: () => _fpsWindowT, fpsDisplay: () => _fpsDisplay }, set: { fpsFrames: v => { _fpsFrames = v; }, fpsWindowT: v => { _fpsWindowT = v; }, fpsDisplay: v => { _fpsDisplay = v; } }, actions: { onNewFps: fps => { if (typeof Engine !== "undefined") { Engine.debug.fpsHistory.push(fps); if |
| `mountWalkAnimTick` | UNHOSTED | `walk-anim` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | let _combatAmbT = 0; const _combatAmbientTick = mountCombatAmbientTick({ get: { ambT: () => _combatAmbT }, set: { ambT: v => { _combatAmbT = v; } }, actions: { isAmbientReady: () => Sfx.isAmbientReady(), setAmbient: (k, f, t, v, fade) => _setAmbient(k, f, t, v, fade) } }); const _skyDayNightTick = mountSkyDayNightTick({ actions: { setTopColor: (r,g,b) => sky |
| `mountWaveEvents` | UNHOSTED | `wave-events` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | const { ambLight: _ambLight, sun } = mountLighting({ THREE, scene, Engine, renderer }); if (typeof WaveManager !== "undefined") { mountWaveEvents({ WaveManager, Engine, THREE, scene, ambLight: _ambLight, sun, get: { score: () => score, heroHp: () => heroHp, HERO_MAX_HP: () => HERO_MAX_HP, perkMaxHpBonus: () => _perkMaxHpBonus, shotsFired: () => _shotsFired,  |
| `mountWaveHudTick` | FACET_OR_KIND | `wave-hud` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. | const _enemyMeshTick = mountEnemyMeshTick({ actions: { getCamYaw: () => camYaw, getPos: id => world.players.get(id) \|\| null, spawnParticles: (u, y, v, n, col, spd, size) => _spawnParticles(u, y, v, n, col, spd, size), spawnClearPos: (r1, r2) => _spawnClearPos(r1, r2), setPos: (id, x, yy, z, u, vv) => world.setPlayer(id, x, yy, z, u, vv), markHudDirty: () =>  |
| `mountWeaponAmmo` | UNHOSTED | `weapon-ammo` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | let currentWeaponId = "pistol"; const _wAmmo = mountWeaponAmmo({ getWeapons: () => (typeof CFG !== "undefined" ? CFG.weapons : []), getActiveWeaponId: () => currentWeaponId, }); const getWeapon = _wAmmo.getWeapon; const getAmmo = _wAmmo.getAmmo; const setAmmo = _wAmmo.setAmmo; const weaponAmmo = _wAmmo.weaponAmmo; |
| `mountWeaponHudTick` | FACET_OR_KIND | `weapon-hud` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. | const _scopeFovTick = mountScopeFovTick({ get: { aimAmt: () => aimAmt, sprintFovAmt: () => _sprintFovAmt, wasSniperScope: () => _wasSniperScope, sniperSavedCamDist: () => _sniperSavedCamDist, camDist: () => camDist }, set: { aimAmt: v => { aimAmt = v; }, sprintFovAmt: v => { _sprintFovAmt = v; }, wasSniperScope: v => { _wasSniperScope = v; }, sniperSavedCamD |
| `mountWeaponPickupTick` | FACET_OR_KIND | `weapon-pickup` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. | const _smokeZoneTick = mountSmokeZoneTick({ actions: { spawnSmoke: (u, y, v) => _spawnParticles(u, y, v, 1, "white", 0.6, 0.9) } }); const _grenadeCrateTick = mountGrenadeCrateTick({ get: { grenadeCount: () => grenadeCount }, set: { grenadeCount: v => { grenadeCount = v; } }, actions: { playSfx: (str, vol) => playSfx(str, vol), showToast: (msg, type, dur) => |
| `mountWeaponSelector` | UNHOSTED | `weapon-selector` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | const getAmmo = _wAmmo.getAmmo; const setAmmo = _wAmmo.setAmmo; const weaponAmmo = _wAmmo.weaponAmmo; const _weaponSelector = mountWeaponSelector({ getCFG: () => (typeof CFG !== "undefined" ? CFG : {}), getActiveWeaponId: () => currentWeaponId, }); const _showWeaponSelector = () => _weaponSelector.show(); const bullets3D = []; |
| `mountWorldBuilderControls` | UNHOSTED | `world-builder-controls` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. |  }); worldBuilder.attachDragDrop(); mountWorldBuilderControls({ worldBuilder, get: { heroPos: () => world.players.get("hero"), camYaw: () => camYaw }, actions: { playSfx, getScriptRunner: () => window.ScriptRunner \|\| null, info: (...args) => console.info(...args), }, }); mountWorldBuilderHotbar({ |
| `mountWorldBuilderHotbar` | UNHOSTED | `world-builder-hotbar` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. |  }, }); mountWorldBuilderHotbar({ worldBuilder, get: { heroPos: () => world.players.get("hero"), camYaw: () => camYaw, gameMode: () => gameMode, mp: () => _mpRef, mpState: () => _mpState, }, actions: { |
| `mountWorldLayout` | UNHOSTED | `world-layout` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | const WD = window.WorldData \|\| {}; const { buildings, buildingBlockers, buildArenaBoundary: _buildArenaBoundary } = mountWorldLayout({ THREE, LayerBoundary, wdBuildings: WD.buildings \|\| [] }); const NPC_DEFS = WD.npcs \|\| []; for (const n of NPC_DEFS) world.setPlayer(n.id, 0, 0, 0, n.u, n.v); |
| `mountWorldScreens` | FACET_OR_KIND | `world-screens` |  | Verify whether existing facet/kind is complete; add parity test or mark gaps explicitly. | const enterScreenMouseMode = (screen, uv) => _screenInteraction.enterScreenMouseMode(screen, uv); const exitScreenMouseMode = () => _screenInteraction.exitScreenMouseMode(); const _worldScreens = mountWorldScreens({ THREE, scene, screenMesh: window.ScreenMesh \|\| null, worldScreens, worldData: WD, get: { deviceBus: () => deviceBus, heroPos: () => world.player |
| `mountXxx` | UNHOSTED | `xxx` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. |  ───────────────────── This file is the game's single entry point. All state lives here as `let` variables; systems receive it through closure-based dependency injection: mountXxx({ get: { foo: () => foo }, set: { foo: v => { foo = v; } }, actions }) "get/set" pairs expose individual state fields without passing mutable refs. "actions" provides callbacks to  |
| `mountXxxTick` | UNHOSTED | `xxx` |  | Create data/legacy spec or native facet from cloned mount source; add semantic proof. | No cloned mount file found; inspect game.html directly. |
