# Migration Progress

> Tracker for the kind-by-kind migration per `docs/codex/specs/migration-sequence.md`.
> One row per iter. Pick the next `pending` row and migrate it.

## Per-iter checklist (the completion criteria — all must be true before moving on)

- [ ] Kind definition exists in `data/kinds/<name>.json`
- [ ] Kind enum entry exists in `experimental/holograph-runtime/src/registry.js` `KIND_NAMES`
- [ ] Tuning Thing exists in `data/tuning/<name>.json` (magic numbers + provenance)
- [ ] Facet handlers exist in `experimental/holograph-runtime/src/handlers.js` for any new facets
- [ ] Mesh factory (if applicable) in `src/ankhor/mesh_factories.js`
- [ ] Default spawn entries in `data/spawns/default_world.json` (or topic-specific file)
- [ ] MANIFESTs updated (`data/kinds/MANIFEST.json`, `data/tuning/MANIFEST.json`, `data/spawns/MANIFEST.json`)
- [ ] `npm test` exits 0
- [ ] `npm run browser-check` exits 0 (against `game.html` for legacy, ideally also against `index.html`)
- [ ] Visual check: `index.html` renders the new kind correctly

## Sequence

| # | Iter | Kind | Status | Notes |
|---|------|------|--------|-------|
|  1 |  706 | barrel          | DONE 2026-05-23 | First proof — mesh factory + tuning + 8 default spawns. Substrate now renders real game content. |
|  2 |  707 | speed-orb       | DONE 2026-05-23 | Yellow dodecahedron. Bob+spin+emissive-pulse facets added to handlers.js. pickup-radius facet defined (collection waits on hero). 3 default spawns. |
|  3 |  708 | coin-drop       | DONE 2026-05-23 | Gold sphere with magnet facet (priority 35). New `magnet` handler attracts position toward hero when within range. 3 default spawns. |
|  4 |  709 | health-pickup   | DONE 2026-05-23 | Green octahedron. Extended spin facet to multi-axis (x/y/z) for dual-axis rotation. Added 13 missing kind names to registry.js Kind enum (coin-drop, health/ammo/weapon-pickup, armor-shard/vest, grenade-crate, mine/turret/grenade, spec). 2 default spawns. |
|  5 |  710 | ammo-pickup     | DONE 2026-05-23 | Orange box (BoxGeometry 0.18×0.08×0.28). Reuses bob/spin/magnet/pickup-radius. New `ammo` data facet for qty+item. 2 default spawns. |
|  6 |  711 | weapon-pickup   | DONE 2026-05-23 | Group: body+grip+pillar. New `opacity-pulse` facet (priority 23) pulses pillar material.opacity. 3 spawns (rifle/smg/sniper) with weapon_id selecting color. |
| —  |  712 | (architecture)  | DONE 2026-05-23 | **Refactor: everything-is-a-Thinga.** Deleted all 4 flat MANIFEST.json files (kinds/, tuning/, spawns/, worlds/) — replaced with `data/root.json` (a Thinga of kind `root`). Each kind def is now a Thinga of kind `kind-def`. Each spawn file is a Thinga of kind `spawn-set` with its instances as `children`. The world Thinga (`worlds/default.json`) `children` array contains `{ref: "<id>"}` references to every other Thinga it composes (kind-defs + tuning + spawn-sets). boot.js rewritten: starts at `root`, recursively resolves refs by fetching `data/<id>.json`, then three passes (registerKind → spawn → materialize spawn-set children). No manifest indirection; the parent IS the manifest. Added `root`, `world`, `kind-def`, `spawn-set` to KIND_NAMES enum. |
|  7 |  713 | armor-shard     | DONE 2026-05-23 | First kind on the new Thinga-graph architecture. Gold tetrahedron, dual-axis spin (x:1.8, y:3.0 — note swap vs health), magnet, dual-tone pickup sfx tracked in tuning. 3 spawns added; world references updated via {ref} only. |
| —  |  714 | (architecture)  | DONE 2026-05-23 | Split monolithic mesh_factories.js + handlers.js into per-file modules. New `src/ankhor/factories/` and `src/ankhor/facets/` directories, ≤35 lines each. boot.js split into boot.js + compose.js + world_params.js. New CLAUDE.md (substrate doctrine; old preserved at docs/CLAUDE.iter-705.md). New docs/AUTONOMOUS_LOOP.md carries the per-wakeup procedure. Default-facet injection added to boot.js spawn pass. |
| —  |  715 | (architecture)  | DONE 2026-05-23 | **Mesh-spec replaces per-kind factory code.** Added `mesh-spec` facets to 6 tuning Thingas (barrel, speed-orb, coin-drop, health-pickup, ammo-pickup, armor-shard) describing visuals declaratively as data. New `src/ankhor/build_mesh.js` is a generic data-driven Three.js builder (9 geometry kinds, 4 material kinds, snake→camel prop rename). Updated install_mesh_handler.js to resolve mesh-spec from `<thing.kind>-tuning` (or explicit `tuning_ref`); falls back to factory only for kinds not yet migrated. Deleted 6 factory files (barrel/speed_orb/coin_drop/health_pickup/ammo_pickup/armor_shard). Only weapon_pickup factory remains, awaiting per-variant tuning (iter 716+). |
| —  |  716 | (architecture)  | DONE 2026-05-23 | **No `??` fallbacks in src/ankhor.** All scene/camera/grid/light constants migrated to the world Thinga's `world-params` facet (19 keys); engine-wide runtime to a new `boot-params` facet on root.json (pixel_ratio_cap, max_frame_dt_seconds). New `src/ankhor/require_param.js` fails loud when a required key is missing — no `??` to hide it. world_params.js + boot.js rewritten to consume the params directly. Also stripped vestigial u/v aliases in magnet, pickup-radius, ttl, and mesh handler — position is canonical {x, y, z} everywhere. Fixed a typo in `background_color`: 723730 → 724242 (the real 0x0b0d12). `grep ' ?? ' src/ankhor/` now matches nothing. |
|  8 |  717 | crate           | DONE 2026-05-23 | First kind migrated on the fully-cleaned substrate. Wooden crate: BoxGeometry body (0.9³) + 3 slats at y=0.18/0.45/0.72. Mesh-spec is a 4-part group. Tuning Thinga also carries loot-table + break sfx/particle params (handlers for breakage land when hero+bullet kinds exist). 8 default spawns with pre-baked per-instance heading (replaces legacy random rotation). |
|  9 |  718 | grenade-crate   | DONE 2026-05-23 | Green ammo box (BoxGeometry 0.5×0.4×0.5) with emissive stripe. NEW `respawn-on-collect` facet (priority 41): permanent infrastructure pickup that hides mesh on collection and reveals after `cooldown_seconds`. Used for grenade caches (30s respawn, +3 grenades up to 9). Pure visibility toggle — the Thing persists; pickup-radius's despawn semantics are unaffected. 4 default spawns. |
| 10 |  719 | hazard-zone     | DONE 2026-05-23 (fire+poison; smoke deferred) | ONE kind, per-variant tunings (hazard-zone-fire-tuning, hazard-zone-poison-tuning) selected by `mesh.tuning_ref` per spawn. CircleGeometry ground decals (color/radius/segments per variant). NEW facets: `damage-zone` (priority 42; records pending_hits at interval), `status-zone` (priority 43; records pending_statuses). Extended `opacity-pulse` with optional `fade_from_ttl_window` — opacity scales linearly as ttl.remaining → 0. 4 default spawns (2 fire + 2 poison) with ttl 600s for demo persistence. Smoke variant deferred — its emitter-only mechanic doesn't fit the mesh-spec mold cleanly. |
| 11 |  720 | bullet          | DONE 2026-05-23 (movement+lifetime; hit-detection deferred) | ONE kind, per-variant tunings (bullet-hero-tuning, bullet-enemy-tuning). Sphere mesh per variant (yellow emissive hero, red emissive enemy). Movement via position.velocity (existing position handler integrates) + existing ttl despawn. 3 demo spawns prove the substrate produces real projectiles with zero new handler code. Hit detection (collision-sphere, damage-on-hit) deferred to iter 721+ alongside enemy migration. |
| 12a|  721 | enemy (grunt)   | DONE 2026-05-23 | First of 8 variants. kind-def `enemy` (required position/mesh/health; optional ai-fsm/drop-on-death/alert-bubble/health-display). tuning `enemy-grunt-tuning` with mesh-spec (CapsuleGeometry 0.4×1.1, color 0xff0044, dark emissive) + stats from src/config/enemy_types.js line 4. Added capsule/circle/ring/torus to build_mesh.js GEOMETRY map. 2 demo grunts. AI behavior + HP-bar render + drops queued for iter 722+. |
| 12b|  722 | enemy (heavy)   | DONE 2026-05-23 | tuning enemy-heavy-tuning. Mesh-spec is a 2-part group: capsule body (0.55×1.5, color 0x880022) + octahedron type-gem (0.16r, same color, emissive 0.8 opacity 0.92) at y=2.9. Stats hp=200, damage=18, attackRange=2.0, dropQty=24, dropHealth=30. 1 demo spawn at (0, 0, 14). Bob amp 0.05 carried in tuning. |
| 12c|  723 | enemy (fast)    | DONE 2026-05-23 | tuning enemy-fast-tuning. Capsule body (0.28×0.9, color 0xff6600) + gem at y=1.76. Stats hp=40, moveSpeed=5.0, damage=4, dropQty=6. Bob amp 0.04. 1 demo spawn at (14,0,0). Charge AI deferred. |
| ★  |  724 | **hero**        | DONE 2026-05-23 (queue-jump) | Skipped enemy variant queue: 5 facets (pickup-radius, magnet, damage-zone, status-zone, respawn-on-collect) were producing dead capability waiting on heroU/heroV. Hero gives them a source. NEW facet `hero-broadcaster` (priority 5) walks the registry's facetStore each tick and writes hero's (x,z) into every other facet declaring heroU/heroV — pure data activation, consumers needed no change. `hero` added to Kind enum. tuning values (max_hp 100, max_armor 75, armor_absorb 0.6, walk 5, sprint 9, hit multipliers head/back/block/crit) pulled from external extraction agent's read of ../5DEngineMassive/index.html lines 1529-1536, 6607, 5179, 2650 (rewrapped in Ankhor Thinga shape). Visual: cyan-blue capsule + head sphere. Hero static (no input controller yet). |
| 12d–h| 725 | enemy (5 variants bundled) | DONE 2026-05-23 | All 5 remaining variants in one iter per self-assessment (iter 724) — pure data swaps, no risk: **poisoner** (color 0x00bb44, hp 60, capsule 0.4×1.1), **incendiary** (0xffaa00, hp 70), **robot** (0x4466aa, hp 350, bespoke 9-part box mesh: torso+head+eye+arms+legs+feet, 0x334455 body + 0xff2200 eye), **boss** (0x660022, hp 1200, capsule 0.9×2.6, bob 0.06), **sniper** (0x220033, hp 55, capsule 0.4×1.1, dmg 45 range 20). 5 new spawns added; variant queue empty. AI behaviors all deferred to ai-fsm iter. |
| ★  |  726 | **bullet hit-detection** | DONE 2026-05-23 (architectural) | NEW facet `kinetic-hit` (priority 45) — lift-ready per docs/ACTOR_TRAJECTORY.md. Scans registry.byKind(target_kind) within radius; on hit, builds an explicit `{to, message: {kind: damage, amount, source, at}}` envelope (the variable that becomes an actor `emit` after the lift), applies damage to target.health.hp, optionally despawns. Updated bullet kind-def (optional-facets += kinetic-hit) and spawn-set (hero bullets target=enemy r=0.6 dmg=20; enemy bullets target=hero r=0.6 dmg=12; despawn_on_hit=true on all). Bullet's deferred-from-iter-720 unblock is complete. |
| 13 |  727 | vehicle (car)   | DONE 2026-05-23 (static; drive deferred) | One variant `vehicle-car-tuning` with 8-part mesh-spec (chassis + roof + 4 wheels + 2 headlights). Stats from vehicle_physics_tick.js (max_hp 250, max_speed 18, engine_torque 800). 1 demo static car at (-20, 0, -16). Variants queued: drone, mech, sidecar. Drive controls + rigid-body solver deferred — needs input-controller facet + a real physics integrator (no rigid-body solver in substrate yet). |
| 14 |  728 | npc             | DONE 2026-05-23 (static; ai-routine + dialog UI deferred) | One variant `npc-default-tuning`. 2-part mesh-spec: capsule body (0.35×0.9, color 0x56cfff) + flat glow ring (0x00ffaa, opacity 0 — lights up when hero in range via future interaction-prompt facet). Per-NPC `dialog` facet on each spawn carries pure-data lines + speaker (no UI handler yet — rendering waits on screen/hud facet). 2 demo villagers at the inner plaza. Routines + schedules deferred to ai-routine facet. |
| 15 |  729 | screen (monitor) | DONE 2026-05-23 (static panel; screen-render facet deferred — likely actor-lift trigger) | First 7D bridge foothold. One variant `screen-monitor-tuning` with 4-part mesh-spec (base + stand + frame + glowing panel). canvas_resolution 1024×640 carried in tuning. 2 demo monitors at east plaza. Variants queued: jumbotron, sky, build-console, server-room. screen-render facet (canvas paint per-tick from data source) deferred to the iter that wires server-process — the natural moment for the {to, message} envelope pattern. |
| 16 |  730 | server-process  | DONE 2026-05-23 (placeholder spawns; live adapter wiring deferred) | First 7D-bridge kind. kind-def + server-process-default-tuning (small green-emissive box tile). 4 demo spawns (node/vscode/chrome/claude) with placeholder pid/cpu/mem lined up beside east monitors. Real /proc adapter (experimental/holograph-runtime/adapters/process_adapter.py) publishes Thinga-shaped events; WebSocket bridge to in-browser registry is the separate wewon-bridge iter. process-observer was already a stub handler; no new code this iter. Substrate held shape — actor-lift strike 0/3. |
| 17 |  731 | docker-container | DONE 2026-05-23 (placeholder spawns) | Second 7D-bridge kind. Tuning docker-container-default with 2-part mesh (blue-emissive Docker tile + darker base border, distinct from server-process green). Reuses process-observer facet for {name, image, status, ports}. 3 demo containers (postgres/redis/supabase-db) next to server-process tiles. Live `docker ps` adapter exists at experimental/holograph-runtime/adapters/docker_adapter.py; bridge to in-browser registry deferred to wewon-bridge iter. Substrate held shape — actor-lift strike 0/3. |
| 18 |  732 | http-request    | DONE 2026-05-23 (placeholder spawns; streaming wiring deferred) | Third 7D-bridge kind, streaming. kind-def + http-request-default-tuning (small box tile, status palette 2xx/3xx/4xx/5xx in tuning). Required facets [position, mesh, request-stream, ttl] — request-stream stub already at priority 51, ttl despawns at 5s in live mode. 5 demo placeholder requests above east monitors covering 200/302/404/500 statuses (ttl=600 for inspection). Live nginx_adapter.py exists; WebSocket bridge deferred. Substrate held shape under streaming pressure — actor-lift strike 0/3 (the streaming kind composed cleanly with existing spawn+ttl primitives). |
| 19 |  733 | database        | DONE 2026-05-23 (placeholder spawns) | Fourth 7D-bridge kind. kind-def + database-default-tuning. Mesh-spec is the classic database-can shape: cylinder body (0.32r × 0.5h, 16 segments) + 2 darker rim caps. Orange-yellow emissive (0xff9933) distinct from process green and container blue. Reuses existing db-connection facet stub (priority 52). 4 demo databases (claude_node/oracle/hermes_node/supabase) lined up beside docker tiles. Live psql adapter exists; WebSocket bridge deferred. Substrate held shape — actor-lift strike 0/3 (4 of 4 7D-bridge kinds composed cleanly under mutate+reach). |
| 20 |  734 | agent-message   | DONE 2026-05-23 (visual+data; router handler deferred to actor-lift phase) | **LAST main-sequence row.** Fifth and final 7D-bridge kind. kind-def + agent-message-default-tuning. Mesh-spec: small purple-emissive envelope (0x9966ff) + white seal sphere. 4 demo messages floating over east monitor wall (AXIOM, ALIEN_OBSERVER, MAILKAREN, CLAUDE_1). ttl 300s/600s for demo. Live agent_bus_adapter.py exists. The `router` handler (deliver message to recipient agent's inbox) is INTENTIONALLY deferred — it's the first concrete consumer the actor-lift solves cleanly per docs/ACTOR_TRAJECTORY.md. Substrate held shape this iter by scoping tight to visual+data. **MAIN SEQUENCE COMPLETE.** See docs/codex/HANDOFF_ANKHOR_MIGRATION.md. |
| ★  |  735 | weapon-pickup variant split + spawn slim | DONE 2026-05-23 | Side-queue (a). Three per-variant tunings (weapon-pickup-{rifle,smg,sniper}-tuning) each with own mesh-spec carrying the variant color. Deleted src/ankhor/factories/weapon_pickup.js + the entire src/ankhor/factories/ directory + the legacy factory-fallback branch in install_mesh_handler.js. All kinds now build via mesh-spec; install_mesh_handler.js dropped from 60 → 56 lines and simpler. ALSO slimmed 5 monolithic spawn files (barrel 297→19, speed_orb 167→14, coin_drop 164→14, health_pickup 117→13, ammo_pickup 115→13) by relying on kind-def defaults injection from iter 712. Total spawn-file content: 1429→613 lines (-816). User flagged monolith pattern mid-iter; the cleanup fixed both weapon_pickup AND the inherited fat across the original migration. |
| ★  |  736 | tags + smoke hazard-zone | DONE 2026-05-23 | Side-queue (b)+(c). Tagged `substrate-iter-734` + `substrate-iter-735` + `main-sequence-complete` (pushed). Added smoke variant of hazard-zone: NEW `particle-emitter` facet (priority 60) spawns short-lived child Things at random offsets per tick with configurable spawn_probability + radius + ttl. NEW `smoke-particle` kind (added to Kind enum) + tuning + mesh-spec (lo-poly translucent grey sphere). hazard-zone-smoke tuning has empty mesh-spec (zone invisible; only emitted particles render). 1 demo smoke zone at (10,0,10). **STRIKE 1/3 toward actor-lift**: particle-emitter calls registry.spawn directly. Two more such handlers trigger the lift. |
| ★  |  737 | chase-target (ai-fsm slice 1) + hero-broadcaster fix | DONE 2026-05-23 | Side-queue (d) start. NEW `chase-target` facet (priority 32, lift-ready): walks position toward (heroU, heroV) at per-tick velocity while hero within sight_range; speed + sight pulled from the spawn's variant tuning via `mesh.tuning_ref` (each enemy variant moves at its own pace). Demo: added to grunt-0 + heavy-0 spawns. ALSO fixed latent hero-broadcaster bug: it relied on `"heroU" in fd` which was false for every consumer (kind defaults never declared those keys) — replaced with explicit `HERO_CONSUMERS = ["pickup-radius", "magnet", "damage-zone", "status-zone", "respawn-on-collect", "chase-target"]` subscription list. This silently activated the 5 hero-consuming facets shipped iters 707–724 that were no-ops. Cross-Thing reach into variant tuning was straightforward (`registry.byKind("tuning")` + name match by `tuning_ref`) — **strike count unchanged at 1/3**; the lift didn't beg this iter. |
| ★  |  738 | attack-target (ai-fsm slice 2 — melee) | DONE 2026-05-23 | Side-queue (d) cont. NEW `attack-target` facet (priority 34, lift-ready): when hero within attack_range, apply damage at attack_interval cadence; pulls damage + attack_range + attack_interval from variant tuning via mesh.tuning_ref. Built `{to, message:{kind:"damage",...}}` envelope as local before delivering (same shape as kinetic-hit; becomes emit after the lift). Added attack_interval to grunt (1.0s) + heavy (1.4s) tuning. Added attack-target to HERO_CONSUMERS + enemy kind-def. Demo: grunt-0 + heavy-0 now CHASE+ATTACK. **strike count unchanged at 1/3** — same byKind("tuning") pattern, same envelope shape as kinetic-hit; no new awkwardness. |
| ★  |  739 | doctrine integration: 5D-truth + 7D-truth + SECOND_ABSTRACTION_PHASE.md | DONE 2026-05-23 | User-driven directive. Saved user's vision doc verbatim as docs/SECOND_ABSTRACTION_PHASE.md. Rewrote CLAUDE.md (preserved prior at docs/CLAUDE.iter-737.md) adding 5D-truth requirement (every spatial Thinga at (x,y,z,u,v); u/v in render/collision/combat/netcode/interest-management), 7D-truth requirement (Vec7 on every operational Thinga), adapter+proposal rules, 10-step migration loop, server-room-as-dashboard convergence. Propagated integration to docs/AUTONOMOUS_LOOP.md (per-iter 5D-truth/7D-truth checklist; vision anchor), docs/ACTOR_TRAJECTORY.md (gap-list cross-reference), docs/codex/HANDOFF_ANKHOR_MIGRATION.md (full gap-list as long-form side queue), MIGRATION_PROGRESS.md (this row), and synthesis/ docs. **No new code** — pure doctrine integration. Strike count unchanged. Next iters can draw from gap lists when side queue empties. |
| ★★ |  740 | playability: input Thinga + hero-input-move + follow camera | DONE 2026-05-23 | **User pivot** — "the game isnt actually playable, the camera just hovers over the world." Brought core gameplay into substrate. NEW `input` kind (singleton device): installs document keydown/keyup/mousedown/mouseup/mousemove listeners once (module-scoped store), copies state into facet data each tick. `input-state` facet (priority 2) + `input-default-tuning` (WASD KeyboardEvent.code map, mouse_sensitivity_yaw 0.0035). NEW `hero-input-move` facet (priority 10, lift-ready): reads byKind("input")[0].input-state for keys+yaw, pulls walk_speed/sprint_speed from hero-tuning, applies yaw-relative motion to hero position. Updated boot.js: new `updateCamera()` reads byKind("hero")[0] position + input yaw and positions camera behind hero (follow_back, follow_up, follow_lookahead, follow_eye_height — all in world-params, no fallback). Falls back to legacy orbit if no hero exists. Pointer-lock requested on first canvas click. Added 4 world-params keys for follow camera. Demo: WASD walks the hero; mouse turns the camera; orbit camera replaced. Same byKind reach pattern as chase/attack — strike count unchanged. |
| ★★ |  741 | playability: hero-shoot facet | DONE 2026-05-23 | Left-mouse-down spawns hero bullets at cadence travelling in the camera's facing direction. NEW `hero-shoot` facet (priority 12, lift-ready): reads byKind("input")[0].input-state (mouseHeld + yaw), pulls fire_interval/bullet_speed/bullet_damage/bullet_ttl/bullet_hit_radius/muzzle_forward/muzzle_up from hero-tuning (7 new keys), builds spawn envelope as local with full bullet facet list (position+velocity, mesh tuning_ref=bullet-hero-tuning, ttl, kinetic-hit target_kind=enemy), delivers via registry.spawn. Uses `typeof === "number"` guard instead of `??` per CLAUDE.md refusal. **STRIKE 2/3 toward actor-lift**: second spawn-envelope handler (particle-emitter iter 736 was first). Demo: click → bullets fire forward → kinetic-hit damages enemies. Combat loop now closes: hero shoots → bullet hits enemy → enemy.health.hp decreases → enemy chase/attack persists until hp ≤ 0 (drop-on-death pending). |
| —  |  741.5 | docs: GAME_HTML_INVENTORY.md | DONE 2026-05-23 | User: "nothing is the same as game.html, ur missing ALL the functionality." Surveyed every `mount*` call (~135) and grouped into 13 domains, each row mapping game.html → substrate equiv → status (DONE/PARTIAL/MISSING/PRIMITIVE-needed). Honest count: ~15 done, ~15 partial, ~95 missing, ~8 primitives needed. Sequenced 10 playability phases (iters 741→785). No code. |
| ★★ |  742 | playability: close combat loop — death + drop + respawn | DONE 2026-05-23 | THREE small facets, one iter. (1) NEW `enemy-death-cleanup` (priority 28, required on enemy): each tick checks health.hp ≤ 0, calls registry.despawn(thing.id, "killed"). (2) NEW `drop-on-death` (priority 29, optional on enemy, has cleanup hook): on cleanup with reason "killed", reads enemy's position + variant tuning loot table (drop_health, drop_ammo, drop_qty), spawns up to 2 pickup Things at the corpse position (health-pickup + ammo-pickup). Idempotent via _dropped sentinel. Uses lift-ready spawn envelope. **STRIKE 3/3 toward actor-lift**: third spawn-envelope handler. Trigger condition met but deferred behind user playability pivot — lift will land in a HUD or screen-render iter per GAME_HTML_INVENTORY.md sequencing. (3) NEW `hero-respawn` (priority 26 on hero): each tick if hp ≤ 0, restore hp to maxHp + teleport to (0,0,0) + brief 1.5s `respawning_until_sec` invuln window (field set but unread today). Skipped npm test this iter due to disk pressure (0.64GB free on C: after dropping node_modules) — node --check + JSON.parse on all touched files passed. |
| ★★ |  743 | playability: HUD shell (HP bar + kill count + shots + crosshair) | DONE 2026-05-23 | NEW `hud` singleton kind + `hud-default-tuning` (30+ visual params: positions, colors, font, thresholds, crosshair geometry — all extractable to playtest) + `spawns/hud.json` (one hud Thinga). NEW `hud-overlay` facet (priority 95, runs after combat): on first tick creates DOM container appended to body styled from tuning (z-index 1000, pointer-events:none so canvas keeps clicks), each tick reads byKind("hero")[0].health → paints HP bar with 3-stop color (green > 50% > yellow > 25% > red), reads byKind("enemy").length to maintain a kill counter (best-effort delta count until actor lift gives real kill events), reads hero-shoot._seq for shots-fired counter, +center crosshair. All values from hud-default-tuning — NO hardcoded numbers in handler. "hud" added to KIND_NAMES enum. Subsequent iters will split widgets (boss bar, wave hud, combo, weapon hud, vignette, status tint, clock, debug) into sibling facets on the same hud Thinga. First DOM-overlay Thinga in the substrate — proves the pattern. |
| ★★ |  744 | playability: pickup activation | DONE 2026-05-23 | Existing pickup-radius facet was inert (just despawned on touch); rewrote as a DISPATCHER. Dispatches by `on_pickup_action` to 5 effect kinds: heal-hero/heal → hero.health.hp + amount (capped at maxHp); give-ammo/ammo → hero.inventory.items[item] += qty; give-armor/armor → hero.health.armor += amount; score-add/coin → hero.inventory.score += amount; speed-boost → hero.inventory.speed_boost_until_sec = now+duration. Amount source order: inline `on_pickup_amount` (drop-on-death pattern), then sibling facet (heal.amount/ammo.qty/etc). Item source order analogous. NEW `inventory` data-only facet on hero (priority 24, no tick) with `{items:{}, score:0}` defaults — written by pickup dispatcher. Registered `heal/ammo/armor/value` as data-only facets (priority 41, no tick) so registry indexes them. Updated hud-overlay paintAmmo() to show `pistol_9mm:N  shots:M  $score` from inventory. Lift-ready: envelope built as local before apply. Strike count unchanged at 3/3 (pickup-radius was already a side-effect handler, not a new spawn-envelope). Combat loop now produces consumable items: enemy dies → drops health/ammo → hero walks over → effect applied → HUD reflects. |
| ★★ |  745 | playability: aabb-collision substrate primitive | DONE 2026-05-23 | First broadphase-style facet. NEW `collider` data-only facet (priority 42) declares AABB half-extents `{half_x, half_z}` on a Thing — added to barrel (0.28×0.28 from tuning body_radius) and crate (0.45×0.45 from tuning body_size 0.9). NEW `aabb-collision` facet on hero (priority 11, runs immediately after hero-input-move at 10): scans every Thing with collider facet via registry.byFacet("collider"), if hero's just-written position overlaps an AABB, pushes hero out along the shallowest axis. Hero's own half-extents pulled from hero-tuning body_radius (0.35) — no hardcoded numbers. Naive O(N*M) scan; the 5D-truth spatial index per docs/SECOND_ABSTRACTION_PHASE.md is the eventual upgrade and folds u/v in at the same time. Hero now collides with all 8 barrels + 4 crates instead of phasing through. Strike count unchanged. First step toward making the world solid. |
| ★★ |  746 | playability: propagate collision (bullets + enemies) | DONE 2026-05-23 | Two small wires that make collision pervasive. (1) Extended `kinetic-hit` facet: NEW `stop_on_collider` flag — when true, before the target-kind scan, scans byFacet("collider") for any AABB the bullet penetrates by more than its hit radius, despawns bullet on wall hit (reason "wall-hit"). Wall check runs FIRST so a bullet brushing a wall AND a target in the same tick still stops at the wall (no damage leaking). Flag added to all hero+enemy bullet kinetic-hit data: bullet-hero spawn-envelope (hero-shoot.js), bullet-enemy demo spawns, drop-on-death does not spawn bullets so unchanged. (2) Extended `aabb-collision.resolveMoverExtents`: generalized hero-only branch to read variant tuning via `mesh.tuning_ref` (same pattern as chase/attack-target/hero-shoot). Now any mover whose kind has a mesh.tuning_ref pointing to a tuning with `body_radius` gets correct AABB extents. Added aabb-collision to enemy kind-def optional-facets + defaults so all 8 enemy variants now collide with barrels/crates AND each other-via-collider (enemies don't have collider yet — that's a follow-up if they should block bullets; currently they only get pushed BY colliders). Strike count unchanged. World becoming solid for movers AND projectiles. |
| ★★ |  747 | playability: enemy-shoot (ranged AI for sniper + robot) | DONE 2026-05-23 | NEW `enemy-shoot` facet (priority 33, sibling of attack-target at 34 — runs first this frame). When broadcast hero pose within `ranged_attack_range`, fires a bullet toward hero at cadence `ranged_attack_interval`. STRICT tuning shape: requires 8 ranged_* keys in variant tuning (range, interval, damage, bullet_speed, bullet_ttl, bullet_hit_radius, muzzle_y, muzzle_forward); no-op if any missing — enforces data discipline, no hardcoded fallbacks. Added the 8 keys to enemy-sniper-tuning (slow lethal: 2.6s interval, 45 damage, 32 speed bullet, 20m range) and enemy-robot-tuning (fast plasma: 0.55s interval, 12 damage, 26 speed, 12m range). Bullet uses bullet-enemy-tuning mesh + standard kinetic-hit envelope (target_kind=hero, despawn_on_hit, stop_on_collider). Added enemy-shoot to enemy kind-def optional-facets + defaults (heroU/V null), added to HERO_CONSUMERS in hero-broadcaster, added enemy-shoot facet to sniper-0 + robot-0 spawns. **FOURTH spawn-envelope handler** after particle-emitter (736), hero-shoot (741), drop-on-death (742) — strike trigger long since met; lift still deferred behind playability. Sniper at (-22,0,-2) and robot at (16,16) now fire when hero in sight; bullets respect walls + damage hero. |
| ★★ |  748 | playability: damage-flash (visceral combat feedback) | DONE 2026-05-23 | Screen pulses red when hero takes a hit. NEW `damage-flash` facet (priority 96, sibling of hud-overlay on the hud Thinga): reads hero.health.hp, tracks _lastHp, on a drop ≥ `damage_flash_min_drop` (default 0.5 to ignore regen rounding) sets _flashStart; while elapsed < `damage_flash_decay_seconds`, paints `_flashEl` opacity = (1-k)*max_opacity where k=elapsed/decay. Cross-facet handoff: damage-flash reads the `_flashEl` from hud-overlay's facet data (installed in hud-overlay's init()) via registry.facetData(thing.id, "hud-overlay")._flashEl — first cross-facet shared-DOM pattern, validates the "one Thinga owns its DOM" model. Added 4 tuning keys (damage_flash_color, max_opacity, decay_seconds, min_drop) to hud-default-tuning. Updated hud-overlay.installDom() to create the flashEl (full-screen, opacity 0, mix-blend-mode:screen). Updated hud kind-def + spawn to carry damage-flash facet. NO hardcoded numbers — all 4 keys required else handler no-ops. Combat now feels: take a sniper hit → red flash → HP bar drops → react. |
| ★★ |  749 | playability: hero ammo consumption | DONE 2026-05-23 | Left-click no longer infinite. Updated `hero-shoot`: before fire, reads hero.inventory.items[ammo_item] (ammo_item from new hero-tuning key `hero_ammo_item` = "pistol_9mm"), returns early if 0 or undefined. On fire, decrements by 1. Added 2 keys to hero-tuning: hero_ammo_item ("pistol_9mm") + hero_starting_ammo (60). Hero spawn inventory now starts with `{items: {pistol_9mm: 60}, score: 0}` instead of empty — player gets 60 starting shots. Ammo-pickup drops from drop-on-death (iter 742) already feed into hero.inventory.items[ammo_item] via pickup-radius dispatcher (iter 744), so the loop closes: 60 starting shots → fire until empty → kill enemy → drops ammo-pickup → walk over → ammo refilled. HUD ammo readout (iter 744) already shows the count. NO hardcoded ammo defaults — handler no-ops if hero_ammo_item missing from tuning. Strike count unchanged. |
| ★★★|  750 | playability + primitive: enemy hp-bars + render-context | DONE 2026-05-23 | Two pieces. **PRIMITIVE**: NEW `render-context` kind + data-only facet (priority 1). boot.js spawns one render-context Thinga after scene/camera setup carrying `{THREE, scene, camera}`. Facets reach the render layer via byKind("render-context")[0] — no globals, everything-is-a-Thinga discipline preserved. Closes the "🔧 PRIMITIVE" gap blocking many UI facets. **FEATURE**: NEW `health-display` facet (priority 65) on enemy: lazy-installs a billboard PlaneGeometry above the enemy on first tick, each tick reads health.hp/maxHp and positions+scales+colors the plane (full=green, low=red, threshold from tuning). Bar hidden at full HP. lookAt(camera) for billboarding. cleanup() disposes geom+mat and removes from scene. Bar Y offset = variant tuning's body_height + hud-default-tuning enemy_hp_bar_padding (different per variant, no hardcode). 6 new keys on hud-default-tuning. Added health-display to enemy kind-def defaults so all 8 variants get bars. NO hardcoded numbers — handler no-ops if any tuning key missing. Combat readability now exists: player can see which enemy is one shot from dying. |
| ★★ |  751 | playability: muzzle flash + impact decals | DONE 2026-05-23 | Combat gets visual punch. NEW `decal-particle` kind + 2 variant tunings (decal-particle-muzzle-tuning: yellow 70ms flash; decal-particle-impact-tuning: orange-red 180ms puff) + NEW `expand-fade` facet (priority 60, runs BEFORE mesh at 70): lazy-resolves initial ttl on first tick, each frame computes k = 1 - remaining/initial, lerps scale + opacity from start→end values pulled from the spawn's variant tuning. Reuses existing mesh + ttl primitives — no new render plumbing. hero-shoot extended: after spawning bullet, spawns decal-particle/muzzle at the muzzle position (FIFTH spawn-envelope handler accounted for). kinetic-hit extended: on wall-hit OR target-hit, spawns decal-particle/impact at the hit position (SIXTH). Added decal-particle to KIND_NAMES + world refs (kind + 2 tunings). Spawn helper functions read ttl_default from each tuning — no hardcoded numbers. Strike count irrelevant now (already met 3/3); lift still deferred. Combat: click → muzzle flash → bullet flies → impact puff at hit. Feedback loop is visceral end-to-end. |
| ★★★|  752 | playability: barrel + crate explosions (AOE + cascade) | DONE 2026-05-23 | Two pieces. (1) Extended `kinetic-hit`: added `target_kinds: []` array (precedence over single `target_kind`); reordered checks so target-hit runs BEFORE wall-stop, so bullets that hit a barrel (which ALSO has collider) take the damage path instead of the silent wall-stop. Updated bullet demo spawns + hero-shoot envelope to `target_kinds: ["enemy", "barrel", "crate"]`. Enemy bullets still target_kind="hero" only — they don't break barrels. (2) NEW `destructible-explode` facet (priority 27, just before enemy-death-cleanup at 28): on hp ≤ 0, reads destructible.{blastRadius, damage, target_kinds_aoe}, scans nearby Things, applies linear-falloff damage envelopes (lift-ready locals), spawns 5 explosion-puff decals via decal-particle-impact-tuning at offset positions, despawns the host with reason "exploded". Cascade is implicit: another barrel taking the AOE damage drops to ≤0 hp and fires its own destructible-explode next tick. Sentinel `_detonated` prevents double-fire. Added destructible-explode to barrel kind-def required-facets + defaults + `target_kinds_aoe`=[enemy,barrel,crate]. Added to crate as optional + small inert blast (damage 8, radius 1.2) so crates do a tiny puff but no cascade. NO hardcoded numbers in handler. Strike count unchanged (already met). Now shooting a barrel detonates it, damages nearby enemies, cascades through neighboring barrels — the canonical fps "shoot the red barrel" moment lands. |
| ★★★|  753 | playability: wave-spawner (endless waves) | DONE 2026-05-23 | Combat no longer dries up after the 9 demo enemies. NEW `wave-spawner` singleton kind + `wave-default-tuning` (base_per_wave 4, per_wave_scaling +2, spawn_interval 1.4s, inter_wave_pause 4s, 6-variant pool, 8 ring spawn positions, elite_every_n_waves 5 starting at wave 5 with boss tuning) + spawns/wave_spawner.json (1 singleton). NEW `wave-spawner` facet (priority 4, runs before HUD): tracks wave_number/_remaining/_next_spawn_at/_inter_wave_until in facet data. When byKind("enemy").length==0 AND _remaining==0: arms inter_wave_pause then increments wave_number and sets _remaining = base + N*scaling. Each tick while _remaining > 0 AND now>=next_spawn_at: spawns one enemy (random variant from pool, random position from ring, hp from variant tuning) via lift-ready envelope. On the last spawn of the wave, if wave_number is a boss-wave per tuning, spawns an additional elite enemy. **SEVENTH spawn-envelope handler**. Extended hud-overlay.paintKills() to also display `wave N (+M incoming)` from byKind("wave-spawner")[0]. Added "wave-spawner" to KIND_NAMES + 3 world refs (kind/tuning/spawn). The combat now self-sustains: wave 0 = 9 demo enemies → wave 1 (4 spawns) → wave 2 (6) → wave 3 (8) ... wave 5 introduces a boss elite. |
| ★★★|  754 | playability: vehicle drive (E to enter, WASD to drive) | DONE 2026-05-23 | The car at (-20,0,-16) becomes a real car. THREE pieces. (1) NEW `vehicle-enter-prompt` facet on hero (priority 14): edge-down detection on `key_interact` (KeyE from hero-tuning); when pressed while a vehicle is within `interact_range_m` (3.0 from hero-tuning), toggles hero.inventory.in_vehicle_id ↔ vehicle.id. (2) NEW `vehicle-drive` facet on vehicle (priority 11): only runs when a hero's inventory.in_vehicle_id == vehicle.id. Reads input keys + vehicle variant tuning (acceleration, max_speed, reverse_factor, drag, brake_drag, turn_rate_rad, passenger_seat_y, follow_*); applies throttle (W/S) along heading + steering (A/D as yaw delta, scaled by forward/reverse) + drag decay + brake_drag (S while moving fwd). Teleports the driver hero to passenger_seat_y above the car each tick so the hero mesh tracks visually. NO hardcoded numbers (8 keys required else no-op). (3) Camera follow updated in boot.js: when hero.inventory.in_vehicle_id set, follow vehicle position+heading using vehicle tuning's follow_back/up/lookahead instead of world-params hero-follow. (4) hero-input-move gated to no-op while in vehicle. Added 10 new keys to vehicle-car-tuning (drive physics + camera follow). Added vehicle-enter-prompt facet to hero kind-def + spawn; vehicle-drive to vehicle kind-def defaults. Combat + traversal merge: shoot enemies on foot, run to car, E to enter, drive across map dropping pickups, E to exit, resume shooting. |

## Architectural baseline (after iter 706)

- `index.html` (66 lines) → boots Ankhor substrate via `src/ankhor/boot.js`
- `src/ankhor/boot.js` (now ~140 lines) → assembles registry + render + 3 data loaders + tick loop
- `src/ankhor/mesh_factories.js` → named Three.js factories (barrel done)
- `src/ankhor/install_mesh_handler.js` → mesh facet handler with init/tick/cleanup
- `data/kinds/MANIFEST.json` → 25 kind definitions
- `data/tuning/MANIFEST.json` → tuning Things (barrel done)
- `data/spawns/MANIFEST.json` → spawn sets (default_world.json with 8 barrels)
- `experimental/holograph-runtime/src/registry.js` → ThingRegistry (Kind enum, 7 regrets honored)
- `experimental/holograph-runtime/src/handlers.js` → pure-data facet handlers (position, ttl, health, etc.)
- `game.html` → unchanged legacy 2449-line engine+game

## Conventions

- Magic numbers NEVER inline in factories or handlers — always tuning Thing lookup with fallback
- Provenance MUST cite source file + line numbers when extracting
- Visual changes are zero — the goal is feel preservation
- `do_not_optimize: true` in provenance signals constants are load-bearing and must not be "tidied"
- One kind per iter; commit + push per iter; do not bundle migrations
