# Bug Log
# Bugs encountered during play that existing tests did not catch.
# Format per entry:
#
# ## YYYY-MM-DD HH:MM — short description
# **Symptom:** what was observed
# **Root cause:** what was actually wrong (if known)
# **Why tests missed it:** which test shape would have caught it
# **Status:** open | test-written | fixed

# No bugs logged yet.
# When you encounter one during play, append it here (see LOOP_PROMPT.md).
