# Holograph Runtime

Experimental universal 5D/6D/7D graph runtime.

This folder does not replace the existing demo. It prototypes a more abstract
engine where worlds, servers, processes, databases, repos, backups, agents, and
network peers share one node/link graph format.

The default mode is propose-only. The code models state and proposed actions; it
does not mutate production systems.
