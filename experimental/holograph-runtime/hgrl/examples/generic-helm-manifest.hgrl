world "generic-helm-manifest" {
  dimensions { x = release y = dependency z = service u = namespace v = server w = rollout t = revision }
  safety { default_mode = propose_only destructive_actions = forbidden }
}
