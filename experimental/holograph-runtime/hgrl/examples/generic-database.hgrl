world "generic-database" {
  dimensions { x = database y = schema z = table u = service v = server w = backup t = version }
  safety { default_mode = propose_only destructive_actions = forbidden secrets = redact }
}
