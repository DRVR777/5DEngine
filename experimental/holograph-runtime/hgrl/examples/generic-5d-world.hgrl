world "generic-5d-world" {
  dimensions { x = identity y = dependency z = runtime u = world v = machine w = replication t = time }
  safety { default_mode = propose_only destructive_actions = forbidden }
}
