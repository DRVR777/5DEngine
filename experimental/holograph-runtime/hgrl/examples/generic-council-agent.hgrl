world "generic-council-agent" {
  dimensions { x = agent y = evidence z = proposal u = task v = server w = coordination t = run }
  safety { default_mode = propose_only destructive_actions = forbidden secrets = redact }
}
