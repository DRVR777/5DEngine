world "generic-server" {
  dimensions { x = identity y = dependency z = runtime u = world v = server w = replication t = time }
  safety { default_mode = propose_only destructive_actions = forbidden secrets = redact }
}
