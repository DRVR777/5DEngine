{
  "id": "legacy/heartbeat",
  "kind": "legacy-system",
  "name": "legacy_heartbeat",
  "facets": [
    {
      "name": "legacy-mount",
      "data": {
        "module_url": "./src/systems/heartbeat.js",
        "export": "mountHeartbeat",
        "tick_args": [
          "@dt"
        ],
        "bindings": {
          "get.heroHp": "$kind:hero[0]/health/hp",
          "get.HERO_MAX_HP": "$kind:hero[0]/health/maxHp",
          "get.heroDead": "$const:false",
          "get.heartbeatT": "$thing:legacy/heartbeat/legacy-mount/_heartbeatT",
          "set.heartbeatT": "$thing:legacy/heartbeat/legacy-mount/_heartbeatT",
          "actions.playSfx": "$log:[heartbeat sfx]"
        }
      }
    }
  ],
  "migration": {
    "legacy_mount": "mountHeartbeat",
    "status": "NATIVE_VERIFIED",
    "semantic_test": "Threshold gate + reset formula + clamp — hp 20 < 30 fires _heartbeatT = 0.38 + (hp/threshold)*0.82 = 0.9267; countdown 0.5s decays to 0.4267; hp 80 ≥ threshold clamps to 0. $log:[heartbeat sfx] confirms actions.playSfx invocation (test_legacy_bridge.mjs iter-777 phase).",
    "native_target": "heartbeat",
    "semantic_iter": 777,
    "native_test": "src/ankhor/facets/heartbeat.js reproduces threshold gate, reset formula, countdown, clamp, and SFX volume formula through hero.inventory heartbeat state (test_legacy_bridge.mjs native heartbeat phase).",
    "native_iter": 778
  }
}
